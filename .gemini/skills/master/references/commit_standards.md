# 📝 Commit & Documentation Standards

1. **Conventional Commits**: `feat:`, `fix:`, `docs:`, `refactor:` 접두사 사용.
2. **의미 있는 메시지**: "Update code" 대신 "fix(logger): handle empty log file error"와 같이 구체적으로 작성.
3. **자동 동기화**: 모든 코드 변경은 `CHANGELOG.md`와 `PLAN.md`에 반영.
4. **이력 관리**: `git log -n 5`를 통해 기존 스타일과 일관성 유지.
