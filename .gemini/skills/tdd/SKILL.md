---
name: tdd
description: RED-GREEN-REFACTOR 사이클로 테스트 주도 개발을 진행합니다.
---

# 🧪 TDD 프로토콜 (Test-Driven Development)

코드를 먼저 작성하지 마십시오. **실패하는 테스트를 먼저 작성**하고, 그것을 통과시키는 최소한의 코드를 작성합니다.

## RED → GREEN → REFACTOR 사이클

### 🔴 RED: 실패하는 테스트 작성

1. 구현할 기능의 **기대 동작**을 테스트로 먼저 작성합니다.
2. 테스트가 **실패**하는 것을 확인합니다. (아직 구현이 없으므로 당연히 실패해야 함)
3. 테스트가 **올바른 이유**로 실패하는지 확인합니다.

```python
# 예시: 아직 구현 안 된 함수 테스트
def test_add_user():
    result = add_user("홍길동", "hong@test.com")
    assert result["status"] == "success"  # ← 이 시점에 실패해야 정상
```

### 🟢 GREEN: 테스트를 통과하는 최소 코드 작성

1. 테스트를 **통과시키는 가장 단순한** 코드를 작성합니다.
2. 깔끔하지 않아도 됩니다. 일단 통과가 목표입니다.
3. 모든 테스트가 **GREEN**이 될 때까지 반복합니다.

### 🔵 REFACTOR: 코드 정리

1. 테스트가 모두 통과하는 상태에서 코드를 정리합니다.
2. 중복 제거, 명확한 이름, 단일 책임 원칙 적용.
3. **리팩토링 중에도 테스트는 항상 GREEN이어야 합니다.**

## 테스트 작성 원칙

- **단위 테스트**: 함수/메서드 하나씩
- **경계값 테스트**: 빈 값, 최대값, 음수 등 엣지 케이스 반드시 포함
- **테스트 이름**: `test_{기능}_{조건}_{기대결과}` 형식
- **독립성**: 테스트 간 상태 공유 금지 (각 테스트는 독립적으로 실행 가능해야 함)

---
**마지막 업데이트:** 2026-02-25 - [Vibe Coding] 자체 스킬로 통합
