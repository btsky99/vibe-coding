# 음성 사이드카 전용 의존성.
#
# [🔴 앱 venv 에 섞지 말 것] 이 목록은 .ai_monitor/voice-server/.venv 에만 깔린다.
#   앱 venv 와 합치면 onnxruntime/ctranslate2 가 앱 의존성과 버전을 다투고, 음성을 안 쓰는
#   사용자까지 200MB 넘는 설치를 떠안는다. 프로세스를 가른 이유가 그것이다(voice_server.py 헤더).
#
# 설치:
#   python scripts/setup_voice.py    (venv 생성 + 아래 설치 + 임포트 검증까지)
#   또는 수동:
#   uv venv .ai_monitor/voice-server/.venv --python 3.11
#   VIRTUAL_ENV=.ai_monitor/voice-server/.venv uv pip install -r .ai_monitor/voice-server/requirements.txt
#
# [제약 — 파이썬 3.11] ctranslate2 휠이 3.13 에 아직 없다(2026-08 기준). 3.12 는 되지만
#   실측한 조합은 3.11 이다.

# 받아쓰기(STT)
faster-whisper==1.2.1

# 낭독(TTS) — 유일한 엔진. 마이크로소프트 Edge 읽어주기를 그대로 부른다.
# [WHY 이것만 남았나 — 2026-08-15 실측] 로컬 후보 6종이 전부 떨어졌다(Kokoro 는 한국어
#   목소리가 0개). 모델 0개·GPU 0·문장당 0.5~1.1초. 근거는 engines/tts_edge.py 헤더.
# [🔴 네트워크가 필요하다 — 되돌아갈 길은 여기 없다] 2026-08-15 정리로 sherpa-onnx(28MB)
#   와 pywin32(SAPI)를 걷어냈다. 망이 끊겼을 때 읽는 것은 **프론트의 브라우저 내장
#   합성기**(vibe-view/src/lib/browserVoice.ts)다 — 이 venv 밖이라 여기 적을 것이 없다.
#   무음이 되는 것이 이 스택에서 가장 나쁜 결과이므로, 그 경로를 지우지 말 것.
edge-tts==7.2.8

# [🔴 numpy·onnxruntime 을 여기 다시 적지 말 것] 예전에는 sherpa 때문에 명시했지만,
#   지금은 faster-whisper 가 제 의존성으로 끌어온다(onnxruntime<2,>=1.14 → numpy).
#   중복해서 핀을 박으면 STT 가 요구하는 범위와 다투게 된다. 즉 **듣기 쪽 부품**이다 —
#   낭독을 정리한다고 같이 지우면 마이크가 죽는다(2026-08-15 실제로 지울 뻔했음).
