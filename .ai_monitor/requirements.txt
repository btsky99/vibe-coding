pycryptodome==3.23.0
# [2026-07-24] 플랫폼 마커 필수 — 마커가 없으면 macOS/Linux에서 `pip install -r`이
# 이 줄에서 실패해 CI 첫 스텝부터 정지한다(pywinpty는 Windows 전용 휠만 존재).
# pyproject.toml은 pythonnet/pywin32에 이미 마커를 달아뒀는데 이 파일만 누락돼
# 두 곳이 어긋나 있었다. 새 의존성 추가 시 양쪽 동기 확인할 것.
pywinpty==3.0.3; sys_platform == 'win32'
# macOS 네이티브 메뉴/클립보드(app_boot.py의 AppKit·PyObjCTools) 의존성.
# 코드는 2026-07-22 맥 대응 커밋에서 들어왔으나 의존성 선언이 빠져 있었다.
pyobjc-framework-Cocoa>=10.0; sys_platform == 'darwin'
rich==14.3.3
textual==8.0.0
watchdog==6.0.0
websockets==16.0
aiohttp==3.12.15
pyinstaller>=6.0,<7.0
pywebview==6.1
PySide6==6.8.2
fastembed
numpy
chromadb
python-dotenv==1.2.1
# [2026-07-15] 2.9.9→2.9.11 — Python 3.13 휠(cp313)은 2.9.10부터. CI 3.13 승격과 동기.
psycopg2-binary==2.9.11

# ── 음성(낭독 TTS / 받아쓰기 STT) ─────────────────────────────────────────────
# [🔴 왜 앱 의존성으로 올라왔나 — 2026-08-16 사고] 원래 이 둘은 별도 환경
#   (.ai_monitor/voice-server/.venv)에만 깔렸다. 그 환경을 **만들어 주는 사람이 설치본에는
#   아무도 없었다** — setup_voice.py 를 부르는 곳이 설치·최초실행 어디에도 없어서,
#   설치본에서는 사이드카가 영영 못 뜨고 마이크도 목소리 목록도 조용히 죽었다.
#   (증상: 단추는 눌리는데 전송 안 됨 + edge-tts 목소리가 안 보임)
# [WHY 런타임 설치가 아니라 동봉인가] 사용자 결정 2026-08-16. 처음 쓸 때 받게 하면
#   설치본은 가볍지만 첫 사용이 네트워크에 걸린다. 깔면 바로 되는 쪽을 택했다.
# [대가] 설치본이 약 190MB 커진다(av ~126MB, ctranslate2 ~60MB). onnxruntime·numpy·
#   tokenizers 는 fastembed 가 이미 끌고 와 중복 비용이 없다.
# [동기 의무] 여기를 바꾸면 vibe-coding.spec hiddenimports ↔ build-release.yml
#   --hidden-import ↔ boot.py `if False:` 블록을 함께 갱신할 것(과거사고 v3.7.215~218).
edge-tts==7.2.8
faster-whisper==1.2.1
