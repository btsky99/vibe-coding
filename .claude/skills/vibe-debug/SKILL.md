---
name: vibe-debug
description: >
  버그의 근본 원인을 4단계로 분석합니다. 증상이 아닌 원인을 수정합니다.
  Use when: 오류, 에러, 버그, "안 돼", "안됨", "왜 안되", "고쳐줘", 예외(Exception), 스택 트레이스 발생 시.
allowed-tools: Agent
user-invocable: true
---

<!--
FILE: .claude/skills/vibe-debug/SKILL.md
DESCRIPTION: 버그 분석 요청 시 debugger subagent로 위임하는 래퍼 스킬.
             메인 컨텍스트 다이어트 + 4단계 분석 격리 + Edit 권한 격리가 목적.

REVISION HISTORY:
- 2026-06-07 Claude: subagent 래퍼로 전환 (이전: 메인에서 직접 분석/수정)
  - .claude/agents/debugger.md 와 매핑 (Edit 권한 있음 — 가드 필수)
  - 라우팅 정책: .claude/agents/README.md 참조
-->

# 디버깅 — Subagent 위임 래퍼

당신은 **즉시 Agent 도구를 호출**합니다. 메인에서 스택 트레이스를 직접 분석하지 않습니다.

## 호출 사양

```
Agent(
  subagent_type: "debugger",
  description: "버그 근본 원인 분석 (4단계)",
  prompt: """
    작업 디렉토리: <호출 시점의 cwd — pwd로 확인. 절대 경로 하드코딩 금지>
    증상: <에러 메시지 전문 + 스택 트레이스 + 재현 조건 전달>
    사용자 요청 원문: <전달>

    출력: 한국어
    절차: 4단계 분석 프로토콜
      1단계: 증상 수집 (에러 전문 + git log -10 + 재현 조건)
      2단계: 가설 수립 (원인 후보 3개 이상, 가능성 높은 순)
      3단계: 검증 (최소 재현 코드 + 가설별 확인/반박)
      4단계: 근본 원인 확정 + 수정 방안 제안

    **중요 — 수정 권한 가드 (세분화):**
    - 허용: 진단용 임시 로그/print 추가 (3단계 검증 목적, 이후 제거)
    - 금지: 비즈니스 로직 수정 + 수정 커밋 + 파일 영구 변경
    - 4단계 "수정"은 **수정 방안 제안**이며, 실제 적용은 사용자 승인 후 메인 에이전트가 수행한다.
    - 임시 진단 로그라도 보고 시 "이 줄은 진단 목적으로 추가했으며 제거 필요" 명시.

    결과 형식:
      🔍 근본 원인 (Why): 1줄
      📍 위치: 파일:줄
      💡 수정 방안 (How): 1~2줄, 최소 변경
      🧪 검증 가설: 어떤 가설을 통과해 결론에 도달했는지
  """
)
```

## Subagent 결과 수신 후 (메인 보고 형식)

1. **근본 원인 1줄** (subagent의 Why 그대로)
2. **위치 + 수정 방안** (파일:줄 + 1~2줄)
3. **"이대로 수정할까?"** 대기 — 사용자 승인 시 메인이 Edit 도구로 직접 수정

## 다음 단계 안내
- 수정 완료 후 회귀 방지: `/vibe-tdd`
- 계획 실행 중 에러였다면: `/vibe-execute-plan` 재개
