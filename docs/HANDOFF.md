<!--
FILE: docs/HANDOFF.md
DESCRIPTION: 바이브 코딩 **인수인계 문서**. 세션(창)이 바뀌어도 처음부터 파지 않도록
             「지금 어디까지 왔나 · 지금 막힌 것 · 이미 해 봤는데 아니었던 것」만 적는다.
             코드 구조·파일 목록은 여기 쓰지 않는다 — PROJECT_MAP.md 와 git 이 이미 안다.

             [🔴 손으로 고치는 문서다 — 자동 생성물이 아니다]
               HIVEMIND.md(generate_hivemind_doc.py:24) · PROJECT_MAP.md(generate_project_map.py:759)
               · wiki/(wiki_build.py) 는 전부 스크립트가 덮어쓴다. 인수인계를 거기 적으면
               다음 실행에 사라진다. 그래서 이 자리를 따로 만들었다.

REVISION HISTORY:
- 2026-08-19 Claude: 최초 작성 — 사장 지시("사람이 인수인계하듯이"). 창을 세 번 닫았다
  열 때마다 처음부터 파던 것이 방아쇠. 크립토 팀장 규칙(요약 대신 문서 경로)을 본떴다.
-->

# 인수인계 — 바이브 코딩

> 🔴 **창이 열리면 이 문서부터 읽는다. 그것이 인수인계다.**
> 🔴 **요약과 이 문서가 어긋나면 이 문서가 이긴다. 이 문서와 코드가 어긋나면 코드가 이긴다.**
> 🔴 **일이 끝날 때마다 2·3·4장을 고친다.** 안 고치면 낡은 지도가 되고, 낡은 지도는 없는 지도보다 나쁘다.

마지막 갱신: **2026-08-21** · 현재 판: **v3.7.351** · 기준 커밋: `4df3c5f`

---

## 0. 30초 브리핑

| 질문 | 답 |
|---|---|
| 무엇을 만드나 | AI 여러 대를 한 화면에서 부리는 데스크톱 앱(윈도우). 진짜 목표는 기능이 아니라 **자가 치유** |
| 지금 큰 줄기 | ① 경량화(끝) ② LLM 위키 전환(W12·W13 남음) ③ 음성(사장님 PC 목소리 확인 남음) |
| 제일 위험한 것 | 🔴 **main 에 push 하면 그것이 곧 배포다** — 사용자 PC 전부에 깔린다 |
| 결재 없이 되는 것 | 커밋만. **푸시·배포는 사장 결재 필수** |
| 「배포 완료」의 뜻 | 🔴 **다른 PC(아픽스3)에 깔고·열고·눌러 봤다** — CI 초록불은 완료가 아니다(결재 17) |
| 혼자 하지 말 것 | 세기·찾기·표 만들기는 `check` 팀원(sonnet)에게 넘긴다 |
| 🔴 어디까지 넘기나 | 선은 「코드 수정이냐」가 아니라 **「원인을 아느냐」**다 — CLAUDE.md 규칙 13. **원인 찾기를 소넷에게 시키면 더 느리고 더 비싸다**(실측 316.8초/121.7k vs 215.1초/83.3k) |
| 시간을 제일 많이 먹는 것 | 이미 무죄로 밝혀진 용의자를 다시 파는 것 → **4장을 먼저 읽어라** |

---

## 1. 이 프로젝트가 무엇인가

AI 에이전트(클로드·코덱스 등) 여러 대를 한 창에서 띄우고, 그들이 한 일을 기록·공유하게
만드는 **윈도우 데스크톱 앱**이다. 화면은 React, 속은 파이썬 서버, 기록은 PostgreSQL,
터미널은 Node PTY 로 돈다. 겉보기엔 대시보드지만 **궁극 목표는 기능 확장이 아니다** —
세션이 끊겨도 다음 세션이 이어받고(연속성), 한 번 고친 에러를 두 번 고치지 않는 것
(재발 방지). 즉 **삽질 빈도를 줄이는 것**이 이 프로젝트의 존재 이유다.

방향은 **라이트하게**다(2026-08-14 사장 결정). 쓰지 않는 계층은 남기지 않는다 —
그래서 디스코드(`c6f3cda`)·아픽스 연동(`346a33d`)·관제 보드(`203c9f4`)를 전부 걷어냈다.

---

## 2. 🔴 지금 어디까지 왔나 (2026-08-19)

| 갈래 | 상태 | 근거 |
|---|---|---|
| **경량화 (결재 42)** | ✅ 1~3단계 끝·푸시됨. 볼트 4,452장 접음 | `8fcde46`(드라이브 미러 끄기) · `1cbeff2`+`b0ad511`(위키 색인) |
| ↳ 4단계(PG→SQLite) | 🛑 **금지.** 승인 문면 밖이다. 손대지 마라 | — |
| **LLM 위키 전환** | 🔄 W1~W11 끝, **W12·W13 남음** | `ai_monitor_plan.md` 태스크표 |
| ↳ 새 위키 색인 | ⚠️ **만들었지만 아직 회상 경로에 안 붙었다** (만들었다 ≠ 쓰인다) | `scripts/wiki_index_build.py` |
| **음성 입출력** | ✅ 배포됨(v3.7.343~). 기본 목소리는 edge | `.claude/rules/voice-expected.md` |
| ↳ 사장님 PC 목소리 | ⚠️ **본인 목소리(`qwen:apix`)로 나야 정상.** edge 로 떨어지면 위반 | 같은 문서, 기계 판정법 3가지 |
| **지식 파이프라인** | 🔄 축1·2·4 끝, **축3 남음**. 순서 고정 축2→축1 | `c8e254c` |
| **자가 치유 4장치** | ⚠️ 계측은 🟢인데 **그건 개발 체크아웃 얘기다.** 설치본은 사실상 0 | 훅이 `python D:/vibe-coding/...` 절대경로를 박고 있음 |
| **server.py 분할** | 🔄 R17~R20 끝(2273→1887줄). **1500 은 아키텍처상 불가**, 현실 바닥 ~1839 | — |
| **훅이 도구 실행을 막던 것** | ✅ 고침 | `94f466d` |
| **훅이 매 프롬프트마다 12.3초를 먹던 것** | ✅ 고침 (0.002초) | `e5ef260` |
| **훅 두 개 중 큰 놈(hive_hook)이 14.3초** | ✅ 고침 (2.3초). 회귀 실측까지 함 | `502ea41` |
| **알림 이모지가 훅을 죽여 rc=1+stderr 를 뱉던 것** | ✅ 고침 | `502ea41` |
| **훅 기록이 9000 고정 탓에 조용히 새던 것** | ✅ 고침. 2026-07-16 청산 누락분 | `117a131` |
| **안전장치가 병렬 직원끼리 서로를 막던 것** | ✅ 고침. 차단 사유도 이제 보인다 | `71ca8eb` |
| **훅 지연 최종** | ✅ **14.3초 → 1.3~1.7초** | `79972c2` |
| 🔴 **작업 기록이 08-18부터 나흘째 통째로 죽어 있던 것** | ✅ 고침. 폴백이 한글을 CP949 로 넘기고 있었다 | `4df3c5f` |
| **PG 죽으면 좋은 위키를 덮던 것** | ✅ 고침 | `96eec10` |
| **집짓기(2026-08-19 사장 지시)** | ✅ `check.md` 신설(`model: sonnet`) · 절대규칙 11·12 · 이 문서 이름 표준화 · `/vibe-verify-installed` 6단계 | 아래 3장 없음 |
| **서버 위키 읽기** | ✅ 된다. `apix-wiki` = `C:\Users\com\.local\bin\apix-wiki.cmd`, 서버 57장 | 🔴 `.cmd` 는 cp949 라 깨진다 → 아래 6장 8번 |

---

## 3. 🔴 지금 막힌 것 · 다음 할 것

이어받는 사람은 **위에서부터** 손대면 된다.

1. **08-15~08-17 의 기울기 — 파 봤고, 결함이라는 증거는 안 나왔다. 재의심 금지.**
   2026-08-21 조사 결과를 그대로 남긴다:
   - 시간대 분포는 「서버가 떴다 꺼졌다」로 설명되지 않는다(08-15 는 9시간에 걸쳐
     시간당 2~9건으로 **고르게 얇았다**). 정상일 08-14 는 시간당 122~255건 덩어리.
   - 🔴 **「파일 수정 기록 0건」은 결함이 아니다.** Edit/Write 도구를 안 쓰면 당연히 0 이다.
     확인법: 오늘 세션에서 Edit 0회·Write 1회 → `pg_logs` 도 정확히 0·1 이었다.
   - 🔴 **같은 기간에 Edit 로깅은 멀쩡히 작동했다** — 08-16 에 `[수정 시작]` 39건.
     08-15 만 0 인 것은 그날 그 도구를 안 썼다는 뜻이다. 이것이 제일 강한 증거다.
   - 길이도 무죄다. 이 기간 `task` 최댓값 191자·500자 초과 0건 —
     `_snippet(...,50)` 이 애초에 잘라서 넣는다(`hive_hook.py:781-782`).
   - `project_id` 필터 탓도 아니다(필터 없이 세도 숨은 프로젝트 없음).
   - 그 커밋 31건은 스크립트 무더기가 아니라 02:34~23:27 에 걸친 **실제 기능 작업**이다.
   - 🔴 **남은 단서 하나** — `[커밋 시작]` 이 31건 중 **1건**뿐이다. `git commit` 이
     Bash 도구를 거쳤다면 31건이 남는다(`hive_hook.py:831`). 즉 그날 작업 대부분이
     **Claude 의 Bash 도구를 안 거쳤다.** 유력한 통로는 **앱 자체의 git 기능**
     (`api/git_api.py`) — 그쪽으로 커밋하면 이 훅은 애초에 안 돈다. **미확인.**
   → 다시 팔 가치는 낮다고 판단했다. 파려면 위 마지막 줄부터 시작해라.

2. 🔴 **08-18~08-21 나흘치 기록은 복구 불가다.** 그 기간의 자가 치유 계측(🟢)과 회상
   원료에는 **근거가 없다.** 그 기간을 근거로 무언가를 판단하지 마라.

3. **위키 색인이 회상에 안 붙어 있다** — 낱말(FTS5)+뜻(임베딩) 하이브리드 색인을 만들어
   놓고, 정작 세션 시작 시 주입되는 회상 경로에는 연결을 안 했다. **만들었다 ≠ 쓰인다.**
   → 다음 한 걸음: 회상 호출부가 이 색인을 타게 배선하고, `python scripts/recall_quality.py --sweep`
   으로 관련/무관 간격이 나빠지지 않는지 실측.
   🔴 이 색인은 **pgvector 가 아니라 색인 파일**에 벡터를 뒀다(PG 금지 조건 때문).
   🔴 **빈 결과 허용이 필수다** — 벡터는 0점을 못 낸다.

4. **W12(옛 창고 삭제)** — `zettel_notes` 3969 + `hive_memory` 399 + `.zettel-vault/` 62파일.
   🔴 **W8(회상 품질 확인) 통과 뒤에만.** 순서를 뒤집으면 비교 대상이 사라진다.
   → W13(문서 갱신)은 W12 뒤.

5. **자가 치유가 설치본에서 0** — 발화 주체가 앱이 아니라 CLI 훅인데, 훅이 개발 경로를
   절대경로로 박고 있다. `install_hive_hooks` 를 **자동 호출하는 코드가 없다**(복붙
   프롬프트로만 존재). → 앱 부팅 시 1회 설치를 배선할지 판단 필요.

6. **사장님 PC 목소리 실측** — 귀로 판정하지 마라. `/api/voice/status` 에 `ref` 키가
   있는지, moss 와 edge 합성물의 **바이트 수가 다른지**로 잰다. 같으면 조용한 폴백이다.

7. **지식 파이프라인 축3** — 순서 고정(축2 추출 → 축1 절단). 뒤집으면 적중률 0.

8. 🔴 **아직 안 터진 지뢰 — 목소리 GPU 못을 코드가 안 지킨다.** 사장님이 박으신 한도는
   목소리 **전체 합쳐 3,584MB**(`voice-server/voice_server.py:362`)인데, 기본값
   `FRACTION=0.35` 는 **혼자서도 4,301MB** 라 717MB 초과다(`engines/tts_qwen.py:226`).
   합을 막는 장치가 없다 — `set_per_process_memory_fraction` 은 프로세스마다 걸린다.
   **안 터지는 이유는 `_bake_only_guard` 가 일꾼을 아예 안 띄우기 때문 하나뿐**이다.
   → `VOICE_LOCAL_BAKE_ONLY=0` 을 켜거나 누가 일꾼을 띄우면 **그 순간 깨진다.**
   🔴 값을 그냥 내리지도 마라 — 봉우리 근거를 안 보고 내리면 내 일꾼이 먼저 터진다.
   내릴 때는 합 기준으로 **보드 쪽과 같이** 정한다(각 `0.145`).

---

## 4. 🔴🔴 이미 해 봤는데 아니었던 것 — **여기가 제일 값지다**

> 같은 데를 두 번 파지 않기 위한 층이다. **증상이 비슷하다고 아래 용의자를 다시 의심하지 마라.**
> 아래는 전부 **실측으로 지워진** 것이다.

### 4-1. 무죄가 밝혀진 용의자 (재의심 금지)

| 증상 | 의심했던 것 (❌ 무죄) | 실제 범인 |
|---|---|---|
| 껐는데 앱이 30분마다 되살아남 | **검은 콘솔 창**(그 창의 주인은 예약작업 `APIX Runner` 였고, 부활과는 무관했다) | **고아 `hive_watchdog`.** 헬스체크가 9000~9005만 봤는데 실제 포트는 9000/9002/9004/9006/9008 → 9006·9008은 살아 있어도 늘 '죽음' → 3회 실패 → 30분 쿨다운 → 무한 반복 (`3ac89a6`). 🔴 `server.py` 를 인자 없이 띄우면 **GUI 앱 전체**가 뜬다 — "서버만"이라던 주석이 거짓말이라 추적이 길어졌다 |
| 메시지 보낼 때 창이 축소됨 | ① server.py 중복 실행 ② project_id 매칭 실패 | **`instance_lock` 의 무조건 `ShowWindow(SW_RESTORE)`** + hook_bridge 가 매 메시지마다 server.py 재스폰 (`7fbbff2`). ①은 venv 런처 쌍이라 정상 |
| T1·T2 터미널이 반복 종료 | 하트비트 · PTY 워치독 · node-pty | **크래시가 아니라 리사이클 워처가 죽인 것.** 화석 계측 + 팬아웃이 방아쇠 (`f6e02a4`) |
| 회상이 엉뚱한 걸 물어옴 | 유사도 **임계값** | 임계가 아니라 **순위**. fastembed 풀링 변경으로 간격이 음수가 됨 → e5 전환 + 재임베딩 5169건 |
| API ECONNRESET 반복 | 쿼터(429) · 구 메시 VPN · 포트 고갈 · 옵시디언 루프 · 훅 이중실행 | **전부 배제됨.** 남은 유력 후보 = 금융보안 프로그램의 로컬 프록시(INISAFE/delfino/veraport). **미해결** |
| 데몬 프로세스가 2개씩 보임 | 중복 기동 | **중복 아님.** venv python 은 런처(CPU 0초) + 실제 자식. `ExecutablePath`+CPU 로 판별 |
| "실행해도 아무 일도 안 남" | 코드 결함 | **좀비가 포트를 문 것.** 관리자 kill 이 ok 를 반환해도 안 죽는다. 판정 = `Threads.Count==1` (`24006ac`) |
| `git commit` 이 실패 | git 설정 · PG 설정 | **훅이 stderr 에 한 줄만 써도 하네스가 도구 실행을 차단한다** — 종료코드 0·stdout 비어도 그렇다 (`94f466d`) |
| 훅이 느리다 / 프롬프트마다 멈칫한다 | 훅 본체(DB 쓰기·API 호출) | **포트 탐색이 12.3초.** `urlopen('http://localhost:...')` 이 닫힌 포트마다 IPv6→IPv4 를 차례로 시도해 0.6초씩 쓴다 x 20개. `socket.bind` 로 먼저 걸러 0.002초 (`e5ef260`) |
| 훅이 실패하는데(rc=1) 이유를 모르겠다 | 훅 본체 로직 · DB · 권한 | **알림 문구의 이모지.** cp949 콘솔이 못 찍어 `print` 가 UnicodeEncodeError 로 죽고 트레이스백이 stderr 로 나갔다 (`502ea41`). 🔴 이 저장소는 stderr 한 줄로 도구가 막히던 사고를 이미 밟았다(`94f466d`) |
| 서브에이전트/명령이 이유 없이 거부됨 (`No stderr output`) | 훅 결함 · 권한 · 명령 오타 | **안전장치의 빈도 제한(3초 15회)에 걸린 것.** 카운터 파일이 `%TEMP%` 에 **하나뿐**이라 모든 세션·서브에이전트가 공유했다 → 병렬로 일하면 서로를 막는다 (`71ca8eb`). 사유가 stdout 으로만 나가 화면엔 안 보였다 |
| 훅이 느리다 (2번째 판) | 훅 본체 · DB | **`server_locator.probe_port` 가 닫힌 포트마다 urlopen timeout 을 꽉 채운 것.** 훅 5곳이 공유하는 자리라 이득이 5배 (`79972c2`) |
| 작업 기록(pg_logs)이 안 쌓인다 | 훅 시간초과로 잘림 · 일이 없었음 · PG 미가동 | **폴백이 SQL 을 명령줄 인자로 넘겨 윈도우가 한글을 CP949 로 전달, 서버는 UTF8 을 기대해 거부** (`4df3c5f`). 🔴 나는 처음에 '시간초과' 로 적었다가 틀렸다 — timeout 은 08-20 에 들어갔는데 유실은 08-18 부터였다 |
| 볼트 로직을 고쳤는데 안 바뀜 | 코드 미반영 · 캐시 | **앱이 부팅 시 1회 `exec_module` 해서 옛 코드를 메모리에 들고 있는 것.** 앱 재시작이 유일한 수단 |

🔴 **시험이 성립하는지부터 봐라 — 폴백값이 정답을 흉내낸다.**
`_find_active_server_port` 는 못 찾으면 `return start`(=9000) 를 낸다(`scripts/hook_bridge.py:88`).
그래서 서버가 안 떠 있는 상태에서 신·구 두 판을 비교하면 **둘 다 9000** 이 나와
「같은 답을 냈으니 회귀 없음」으로 읽힌다. 실제로는 둘 다 실패한 것이다.
🔴 **회귀 시험은 반드시 「찾아야 하는 대상이 실제로 있는」 조건에서 해라.**
2026-08-21 에 127.0.0.1:9007 에 진짜 서버를 띄우고 나서야 유효한 대조가 됐다.

🔴 **훅을 잴 때는 `hook_event_name` 을 반드시 넣어라.**
없으면 `hive_hook.py:480` 의 분기가 어디도 안 걸려 **훅 본문이 통째로 스킵된다.**
2026-08-21 에 그 0.2초를 「빨라졌다」로 읽을 뻔했다 — 실제로는 아무 일도 안 한 것이었다.
같은 조건으로 다시 재니 2.3초였다. **아무 일도 안 하는 것은 언제나 제일 빠르다.**

🔴 **속도를 고쳤으면 「기능이 살아 있나」를 같이 재라.**
`server_locator` 는 회상(`recall_client`)도 쓴다. 빨라졌는지만 보고 넘어갔다면
**회상이 죽은 채로 「개선했다」고 보고할 뻔했다.** 판정 기준은 초가 아니라
`[경험 회상]`·`[지식 회상]` 두 블록이 stdout 에 그대로 있는지다(2026-08-21).
같은 이유로 포트 탐색 수정은 **살아 있는 서버를 여전히 찾는지**가 안전선이었다 —
못 찾으면 그건 빨라진 것이 아니라 **눈이 먼 것**이다.

🔴 **「일이 없었다」와 「기록이 샜다」를 가르려면 독립 기준이 필요하다.**
기록만 세면 둘이 똑같아 보인다. 2026-08-21 에는 **git 커밋 수**를 자로 썼다 —
커밋은 훅과 무관하게 남고, 훅은 커밋마다 `[커밋 시작]` 을 남기게 돼 있다.
**커밋은 있는데 그 줄이 없으면 일이 없었던 것이 아니라 샌 것이다.**

🔴 **실패를 세지 말고 실패의 이유를 남겨라.**
`_run_psql` 이 `except: return ''` 로 오류를 삼켜, `Failed to log task to Postgres`
한 줄만 **1,878번** 남았다. 횟수는 넘치는데 원인은 0이라 넉 달을 못 찾았다.
🔴 이 저장소는 스트림이 양쪽 다 막혀 있다(stdout=Gemini JSON-RPC, stderr=Claude Code
차단) — 그러니 **파일에 남겨라.** 못 남길 자리라서 안 남긴 게 아니었다.

### 4-2. 방향 자체가 틀렸던 것 (판단이 뒤집힌 자리)

- 🔴 **"edge 목소리가 났으니 됐다" 는 오독이었다.** 사장님이 원한 것은 소리가 아니라
  **본인 목소리**다. 이 오독으로 며칠을 잃었다 → `.claude/rules/voice-expected.md` 로 못박음.
- 🔴 **"위키는 헷갈리면 덮는다" 가 틀렸다.** 창고가 3969장으로 부푼 원인은 '추가'가 아니라
  **세션 요약을 무분별하게 페이지로 만든 것**이었다. 그 오진 때문에 새 사실이 기존 근거를
  덮어 지식이 조용히 사라졌다 → 지금 기준은 반대다. **헷갈리면 덧붙인다**(2026-08-18 사장 지시).
- 🔴 **W4 원안(`memory/` 98장을 위키로 이관 + MEMORY.md 대체) 폐기.** `memory/` 는 세션마다
  무조건 주입되는 **유일한 100% 활용 층**이라, 검증 안 된 위키로 갈아타는 건 "품질을 재고
  나서 지운다" 원칙을 스스로 어기는 꼴이었다. 원본은 남긴다.
- 🔴 **관제/오케스트레이션 보드 신규 개발 영구 중단.** 대시보드는 런처+기록 열람판이지
  관제판이 아니다(`203c9f4`).
- 🔴 **Jarvis(오라클) 무료 상한은 2코어/12GB 고정.** "4코어/24GB 무료"는 **폐지된 옛 기준**이고
  같은 오답을 2회 반복했다. PAYG 는 초과분을 차단 없이 청구한다.
- 🔴 **server.py 를 1500줄로 줄이는 것은 아키텍처상 불가.** 현실 바닥 ~1839.
  더 쪼개려다 순환 의존을 만들지 마라.
- 🔴 **CipherTrader 게이트에서 "부호만 보고 통과" 는 거짓 보고였다.** 효과크기 0.02 가
  신뢰구간 대역보다 작았다. **게이트 판정에는 항상 CI 를 병기한다.**
- 🔴 **"시험 스킬이 없다"는 틀렸다 — `/vibe-verify-installed` 로 이미 있었다**(2026-08-19).
  만들기 전에 세어 보니 스킬이 14개였다. **새로 만들었으면 15번째가 되어 "어느 걸 봐야
  하나"가 갈렸을 것이다.** → 없는 줄 알고 만들기 전에 **먼저 세라.**
- 🔴 **"학습 중이면 목소리가 위험하다"는 뭉뚱그림이 틀렸다**(서버 위키에서 배움).
  밀리는 것은 **에폭 구간뿐**이고 준비 구간(조립·컴팩션)은 GPU utilization 1~7% 다.
  그리고 **메모리를 더 줘도 안 빨라진다** — 연산은 시분할이라 SM 이 차면 줄을 선다.
  출처: `아픽스/시스템/보드와 음성.md:401`(`81ae9408`) · 같은 장 메모(`3c16d810`).

### 4-3. 🔴 매니저·팀장이 잘못 지시했던 것 (다음에 또 그러지 않게)

- **매번 긴 지시서를 새로 써서 보낸 것.** 프로젝트에 기록이 있는데 가리키지 않고 다시 썼다.
  → 앞으로는 **이 문서를 가리킨다.** 요약 대신 **경로**를 준다(크립토 팀장 규칙).
- **`ai_monitor_plan.md` 에 다른 프로젝트(아픽스) 계획 810줄을 채워둔 것.** 아픽스는 별도
  프로젝트인데 계획서만 남아, 세션을 시작하는 LLM 이 "이게 지금 할 일"로 읽을 수 있었다
  (2026-08-15 사고). 옛 전문은 `git show 88a617c:ai_monitor_plan.md`.
- **승인 문면 밖의 단계를 묶어서 진행하려 한 것** — 경량화 4단계(PG→SQLite)는 결재 42의
  문면에 없다. **결재는 문면까지가 결재다.**
- **"완료"를 오탐으로 보고한 것** — 누적 로그에 남은 옛 `rc=0` 을 읽었다.
  **침묵보다 나쁜 것이 거짓 완료 보고다.** 오프셋 이후만 읽을 것. 사망 ≠ 완료.
- **폴더마다 준 것이 달랐던 것**(2026-08-19 사장 지적) — 어떤 프로젝트엔 규칙·인수인계·
  `check` 팀원이 다 있고 어떤 곳엔 아무것도 없었다. 매니저가 창을 열 때마다 손으로 실어
  보내는 구조라 빠졌다. → **폴더에 박아 둔다.** 지시서로 실어 나르지 않는다.
- **`v3.7.340` 을 「배포 완료」로 보고한 것** — CI 초록불과 릴리즈 발행만 봤다. 깐 판에서는
  마이크도 edge-tts 도 안 됐고, 팀원이 확인한 것은 *이미 돌고 있던 앱*이었다.
  → **결재 17**: 다른 PC(아픽스3)에 깔고·열고·눌러 본 것만 완료다. CLAUDE.md 규칙 11.

---

## 5. 정본이 어디인가 (요약 말고 여기를 열어라)

| 알고 싶은 것 | 정본 | 주의 |
|---|---|---|
| 절대 규칙 10개 | `CLAUDE.md` | 세부 규칙과 충돌하면 **이쪽이 이긴다** |
| 지금 할 일 목록 | `ai_monitor_plan.md` | 다른 프로젝트 계획을 여기 쓰지 마라 |
| 커밋 메시지 형식 | `.claude/rules/commit-rules.md` | 제목만 쓰기 금지, 본문 한글 3단 |
| 파일 1500줄 · LLM 주석 | `.claude/rules/file-limits.md` | 자동 생성물은 제외 |
| 위키 쓰는 법 | `.claude/rules/wiki.md` | **헷갈리면 덧붙인다** |
| 음성 기대 동작 | `.claude/rules/voice-expected.md` | edge 폴백은 정상이 아니다 |
| 실행 그래프("실패하면 어디로") | `docs/AGENT_GRAPH.md` | 새 배선 전에 **먼저** 적는다 |
| 하네스 계약·안전 불변식 | `docs/HARNESS_V2.md`, `docs/HARNESS_DESIGN.md` | — |
| 아키텍처·데이터 흐름 | `.claude/rules/architecture.md` | — |
| 코드가 어디 있나 | `PROJECT_MAP.md` | 🔴 자동 생성 — 손으로 고치지 마라 |
| 과거 사고 원문 | `python scripts/incident.py search "<에러 텍스트>"` | 디버깅 **시작 전에** 조회 |
| 축적된 지식 | `wiki/INDEX.md` (43장) | 🔴 자동 생성 — 원료(주석·사고 장부)를 고쳐라. **여기가 서버 위키의 원본** |
| 남들이 밟은 지뢰 | `apix-wiki search "증상 그대로"` (서버 57장) | 🔴 **고치지 마라** — 쓰는 것은 서버 하나. 새로 안 것은 보고에 적는다 |
| 값싼 팀원 부리는 법 | `.claude/agents/check.md` | 세기·찾기만. 판정·원인 규명은 넘기지 마라 |
| 「됐다」를 확인하는 법 | `.claude/skills/vibe-verify-installed/SKILL.md` | 🔴 **6단계(다른 PC)까지 해야 완료** |

---

## 6. 조심할 것 (이 프로젝트만의 함정)

1. 🔴🔴 **main 에 push 하면 그것이 배포다.** `.github/workflows/build-release.yml` 이
   main push 마다 풀빌드+릴리즈를 돌린다. `.py` 하나만 섞여도 돈다.
   굽지 않고 푸시만 하려면 **머지/헤드 커밋 제목에 `[skip ci]`**.
   🔴 그래도 **설치본엔 닿는다** — soft_updater 는 커밋 SHA 만 본다. 막히는 건 새 EXE 굽기뿐.
2. 🔴 **사람이 안 시킨 실행은 콘솔 창을 띄우지 않는다**(절대규칙 10). `CREATE_NO_WINDOW`
   는 자식에게 **상속되지 않는다** — 말단 호출마다 `infra.proc` 를 거쳐야 한다.
   같은 결함으로 2번 밟았다(`d423a7d`, 2026-08-14).
3. 🔴 **PG(5433)는 앱이 띄운다.** 앱이 꺼진 밤에는 회상·사고장부·체크포인트·훅로깅
   **4장치가 전부 0**이다. "왜 기록이 없지"의 답이 대개 이것이다.
4. 🔴 **포트의 '다음 빈 자리'를 잡지 마라.** LAN 브리지가 9020, 9021, … 로 번진다.
   음성 사이드카가 9021 을 잡았다 조용히 죽어 9030 으로 옮겼다.
5. 🔴 **개발에서 되는 것과 설치본에서 되는 것은 다르다.** config 분리 + DB 공유 때문에
   `project_id` 슬러그가 어긋나 패널이 통째로 비는 사고가 있었다.
   배포 전 `/vibe-verify-installed` 로 실측한다.
6. 🔴 **브라우저가 아니라 PyWebView 네이티브다.** "브라우저 hard reload" 같은 지시는
   성립하지 않는다 — **앱 재시작** 또는 WebView F5.
7. 🔴 **사용자는 개발 비전문가다.** 결론을 먼저, 근거를 나중에. 선택지는 "A안/B안"이
   아니라 상황으로 제시한다(절대규칙 1).
8. 🔴 **글 쓸 때 백슬래시를 Bash 힘드(heredoc)로 넣지 마라.** 2026-08-19 실측 —
   `\vibe-coding.exe` 가 **세로탭으로 먹혀** `VibeCodingibe-coding.exe` 로 저장됐다.
   따옴표 친 `<< 'EOF'` 인데도 그랬다. 화면에는 그럴듯해 보여서 **안 잡힌다.**
   → 윈도우 경로가 든 글은 **Write/Edit 도구**나 **PowerShell** 로 쓴다.
   쓴 뒤 제어문자 0건을 확인한다: `[regex]::Matches($s,"[\x00-\x08\x0B\x0C\x0E-\x1F]").Count`
9. 🔴 **`apix-wiki`(서버 위키)는 `.cmd` 를 그냥 부르면 깨진다** — 래퍼 주석이 한글이라
   cp949 콘솔에서 토막 난다. 파이썬을 직접 부른다:
   `python "C:\Users\com\AppData\Local\Programs\apix-runner\apix_wiki.py" search "..."`
   (`$env:PYTHONIOENCODING="utf-8"` 를 먼저 건다.)
   🔴 **0장과 「못 닿았다」는 다른 것이다.** 종료코드 0 = 서버가 답함(0장 포함) · 3 = 못 닿음.

---

## 7. 이 문서를 살아 있게 유지하는 법

```
일이 끝날 때마다   2장(어디까지) · 3장(막힌 것) · 4장(아니었던 것)을 고친다
새 사실을 알면     새 문서를 만들지 말고 **이 문서를 두껍게** 한다
헷갈리면           덧붙인다. 지운 것은 안 돌아온다
덮을 때는          「무엇이 어떻게 틀렸었는지」를 한 줄 남긴다
```

🔴 **4장에 한 줄 보태는 것이 이 프로젝트에서 가장 값싼 투자다.** 다음 사람이 같은 데를
파는 데 드는 시간이 그 한 줄보다 언제나 비싸다.
