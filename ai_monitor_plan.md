<!--
FILE: ai_monitor_plan.md
DESCRIPTION: 바이브 코딩의 **현재 계획**. CLAUDE.md 가 작업 시작 전에 읽으라고 지정한 문서다.

             [🔴 이 문서에 다른 프로젝트 계획을 쓰지 말 것 — 2026-08-15 사고]
               2026-08-08~12 사이에 이 파일이 아픽스 콘솔/노드/일감 계획 810줄로 통째
               채워져 있었다. 아픽스는 별도 프로젝트인데(사용자 결정 2026-08-14, 코드는
               346a33d 로 철거) 계획서만 남아, 세션을 시작하는 LLM 이 "이게 지금 할 일"로
               읽을 수 있는 상태였다. 옛 아픽스 계획 전문은 git 이력에 있다
               (`git show 88a617c:ai_monitor_plan.md`).

REVISION HISTORY:
- 2026-08-15 Claude: LLM 위키 전환 계획 추가 (Task W1~W13) — 지식 창고를 로그 덤프에서
  백과사전으로. 사용자 승인 2026-08-15.
- 2026-08-15 Claude: 아픽스 계획 810줄 제거 — 별도 프로젝트로 분리, 바이브 코딩 계획으로 환원
-->

# 바이브 코딩 — 현재 계획

## 이 프로젝트가 하는 일

AI 멀티 에이전트 하이브 마인드 대시보드. **라이트하게** 간다 — 쓰지 않는 계층은 남기지
않는다(2026-08-14 방향 결정). 궁극 목표는 기능 확장이 아니라 **자가 치유**다:
세션 연속성 + 재발 방지 = 삽질 빈도 감소.

## 🔴 이 프로젝트에 넣지 않는 것

| 대상 | 이유 | 철거 커밋 |
|---|---|---|
| **아픽스(APIX) 서버 연동** | 별도 프로젝트로 분리. 중앙 대화·일감 카드·노드 상태 전부 해당 | 346a33d, 88a617c 이후 |
| 디스코드 커넥터 | 안 쓰는 계층 | c6f3cda |
| 관제 보드 신규 개발 | 대시보드는 런처+기록 열람판이지 관제판이 아니다 | 203c9f4 |

새 배선을 놓기 전에 `docs/AGENT_GRAPH.md` 에 "실패하면 어디로"를 먼저 적는다.

## 진행 중

### 음성 입출력 (구현 완료 · 실사용 확인 대기)
- 커밋 e2e1c50(기능) + 88a617c(목소리 선택 UI·포트 충돌 수정).
- 기본 목소리 = Windows SAPI **Heami**(명료도 실측에서 sherpa VITS 를 이김).
- 🔴 **남은 것**: 앱 재시작 후 실제 마이크로 말하는 E2E. 설치본에 모델·사이드카 동봉.
- 설치: `python scripts/setup_voice.py`

### 지식 파이프라인 재설계
- 축1(절단)·축2(추출)·축4 구현 완료(c8e254c). e5 전환 + 재임베딩 5169건.
- 🔴 **남은 것**: 축3. 순서 고정 — 축2(추출) → 축1(절단). 뒤집으면 적중률 0.

## 🔥 LLM 위키 전환 (2026-08-15 승인 · 진행 중)

지식 창고를 **로그 덤프에서 백과사전으로**. Karpathy LLM Wiki 패턴을 이 프로젝트에 맞춰
적용한다. 목표는 "사람도 LLM 도 읽는 우리 둘의 백과사전"(사용자 표현).

### 왜 (실측 근거)

| 증상 | 실측 |
|---|---|
| 지식이 안 나온다 | 코드주석 지식 452건의 참조 총합 **5회** |
| 랭킹이 정답을 밀어냄 | 정답 sim 0.887 이 참조 82회짜리 무관 노트에 밀림 → **e8fec8d 로 수정 완료** |
| 창고가 로그 덤프 | 활성 501 건 중 지식 452, 나머지는 세션요약·파일노트 |
| 반면 잘 되는 것 | `~/.claude/.../memory/` **98장은 100% 활용** — 이미 위키 패턴 |

### 3층 구조 (불변식)

```
① wiki/*.md   정본. 사람이 고치고 git 이 버전관리한다
② zettel_notes 인덱스. ①에서 생성되며 언제든 재생성 가능
③ 옵시디언     선택적 뷰어. 미설치 PC 에서도 ①②로 완전 동작해야 한다
```

🔴 **세션 캐시(`hive_memory`·세션요약)는 ①에 절대 넣지 않는다.** 볼트가 로그 덤프가 된
원인이 이 경계를 안 지킨 것이다. 백과사전과 작업기억은 층이 다르다.

### 태스크

```
[ ] W1: wiki/ 뼈대 + INDEX.md + 작성 규약
    파일: wiki/INDEX.md, wiki/{개념,시스템,함정,결정}/, .claude/rules/wiki.md
    방법: 디렉토리 + 프론트매터 규약(title/type/sources/related/confidence) 정의.
          규약은 rules/ 에 두고 CLAUDE.md 는 링크 1줄만(본문 복사 금지 — 규칙 9).
    검증: 빈 위키에서 규약 파일만 읽고 LLM 이 새 페이지를 쓸 수 있는가

[ ] W2: knowledge_extract 를 파일 출력 모드로 확장 (W1 후)
    파일: scripts/knowledge_extract.py (369줄, 기존 재사용)
    방법: --emit-wiki 플래그 추가. 현행 upsert_notes(DB 직행) 대신 주제별로 묶어
          wiki/시스템/<주제>.md 생성. 노트 단위 = 파일이 아니라 **주제**.
    검증: 862개 주석 → 위키 페이지 N장, 각 페이지가 파일:줄번호 출처를 갖는가

[ ] W3: 사고 장부 117건 → wiki/함정/ (W1 후)
    파일: scripts/wiki_build.py (신설)
    방법: incident_ledger 를 증상/원인/수정 3단으로 페이지화. 동일 파일 사고는 한 장에 병합.
    검증: 재발 이력 있는 사고가 한 페이지에 모이는가

[ ] W4: memory/ 98장 편입 (W1 후)
    파일: scripts/wiki_build.py
    방법: ~/.claude/projects/<slug>/memory/*.md 를 wiki/ 로 이관. 이미 위키 형식이라
          프론트매터만 정규화. [[링크]]는 그대로 살린다.
    검증: MEMORY.md 인덱스가 wiki/INDEX.md 로 대체되고 링크가 안 끊기는가

[ ] W5: 위키 → DB 인덱서 (W2·W3·W4 후)
    파일: .ai_monitor/src/wiki_index.py (신설, ~200줄)
    방법: wiki/**/*.md 스캔 → 내용 해시 비교 → 변경분만 zettel_notes upsert + 임베딩.
          knowledge_extract.upsert_notes 패턴 재사용. 파일이 사라지면 인덱스도 제거.
    검증: 위키 1장 수정 → 재인덱싱 → 회상에 즉시 반영

[ ] W6: 인덱서 데몬 등록 (W5 후)
    파일: .ai_monitor/infra/daemons.py (DAEMON_TOGGLES)
    방법: 기존 zettel_sync 데몬 자리를 wiki_index 로 교체. 규칙 10 준수(무창).
    검증: 데몬 패널에 표시 + 토글 동작 + 콘솔 창 안 뜸

[ ] W7: wiki lint (W5 후)
    파일: scripts/wiki_lint.py (신설)
    방법: 페이지가 인용한 파일:줄번호/심볼이 아직 존재하는지 검사. 고아 페이지,
          출처 없는 주장, 150장 상한 초과 경고.
    검증: 일부러 없는 경로를 넣은 페이지가 검출되는가

[ ] W8: 회상 품질 재측정 (W5 후)
    파일: scripts/recall_quality.py (기존)
    방법: 관련/무관 질의로 간격 재측정. _FLOOR·_REF_WEIGHT 가 여전히 타당한지 확인.
    검증: 관련 질의 상위 3건이 전부 위키 페이지인가

[ ] W9: GDrive 허브 미러 (W5 후)
    파일: .ai_monitor/infra/wiki_hub.py (신설), config.json(wiki_hub_path)
    방법: zettel_sync 의 _write_if_changed / mirror_vault 재사용. 프로젝트별 하위폴더로
          분리. 🔴 무조건 write 금지 — 내용 해시 비교 후 변경분만(c7a42f2 핑퐁 사고).
          업로드 API 호출 없음 — 구글 드라이브 앱이 동기화를 담당한다.
    검증: 두 번 연속 실행 시 두 번째는 쓰기 0건

[ ] W10: 리셋 API (W5 후)
    파일: .ai_monitor/api/wiki_api.py (신설), server.py 라우트
    방법: POST /api/wiki/reset — 인덱스 비우기 + wiki/ 비우기 + 원료 재생성.
          설치본에서도 동작해야 하므로 스크립트가 아니라 API.
    검증: 설치본에서 호출 → 위키 재생성 완료

[ ] W11: 리셋 UI 버튼 (W10 후)
    파일: .ai_monitor/vibe-view/src/components/panels/ (지식 패널)
    방법: 확인 다이얼로그 + 진행 표시. 파괴적 작업이므로 2단계 확인.
    검증: 버튼 → 재생성 → 패널에 새 위키 표시

[ ] W12: 옛 창고 전부 삭제 (W8 통과 후에만)
    파일: scripts/wiki_build.py --purge-legacy
    방법: zettel_notes 3969 + hive_memory 399 + .zettel-vault/ 62파일 삭제.
          🔴 반드시 W8(회상 품질 확인) 통과 후 — 순서를 뒤집으면 비교 대상이 사라진다.
    검증: 삭제 후에도 회상 품질이 W8 수준 유지

[ ] W13: 문서 갱신 (W12 후)
    파일: PROJECT_MAP.md, CLAUDE.md, docs/AGENT_GRAPH.md
    방법: 신규 파일 등재 + 위키 규칙 링크 + 실행 그래프에 "인덱싱 실패 시 어디로" 선언
    검증: harness_verify 통과
```

### 의존성

```
W1 ──┬─ W2 ─┐
     ├─ W3 ─┼─ W5 ─┬─ W6
     └─ W4 ─┘      ├─ W7
                   ├─ W8 ── W12 ── W13
                   ├─ W9
                   └─ W10 ── W11
```

🔴 **W12(삭제)는 W8(품질 확인) 이후에만.** 새 위키가 실제로 회상되는 걸 본 뒤에 지운다.

### 이미 끝난 것

- ✅ 회상 랭킹 수정(e8fec8d) — 보정항 0.1→0.004. 정답이 상위에 오는 것 실측 확인.

## 대기 (승인됨 · 미구현)

- **터미널 슬롯별 다른 프로젝트** — PTY 백엔드/서버 override 준비됨, 프론트 2파일 남음
- **LAN 자동 공유** — 자율 판단 파일 전송. 마스터 토글 기본 OFF + 전송 후 명시 리포트 + 민감파일 필터
- **server.py 분할** — R17~R20 완료(2273→1887줄). 1500 은 아키텍처상 불가, 현실 바닥 ~1839

## 알려진 환경 문제 (코드 문제가 아님)

- 🔴 **좀비가 포트를 문다**: `Threads=1` 인 pythonw/python 이 9000·9002 를 붙든 채
  `Stop-Process` 를 튕겨낸다(커널 대기). 락 포트 회수는 24006ac 로 고쳤지만 **HTTP
  서비스 포트**는 여전히 막힌다 — 앱/PC 재시작 외에 수단이 없다.
- 🔴 **포트 지도의 '다음 빈 자리'를 잡지 말 것**: LAN 브리지는 인스턴스마다 9020,9021,…
  로 번진다. 음성 사이드카가 9021 을 잡았다가 조용히 죽었다(→ 9030 이전).
