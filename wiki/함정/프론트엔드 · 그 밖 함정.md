---
title: 프론트엔드 · 그 밖 함정
type: 함정
sources:
  - .ai_monitor/vibe-view/src/components/panels/DaemonsPanel.tsx:39
  - .ai_monitor/vibe-view/src/components/panels/MemoryPanel.tsx:1
related: []
confidence: high
updated: 2026-08-15
---

# 프론트엔드 · 그 밖 함정

## 한 줄

/api/daemons는 신규 라우트라, 라우트 추가 이전에 뜬 서버에 붙으면

> 코드 주석에서 자동 합성 (원료 2건 · 파일 2개 · 추출 7cbf195).
> 🔴 **여기를 고치기 전에** 원본 주석을 먼저 고칠 것 — 다음 빌드에 덮어써진다.

## `.ai_monitor/vibe-view/src/components/panels/DaemonsPanel.tsx`

### pendingNote `[과거사고]`

/**
[과거사고 2026-08-01] /api/daemons는 신규 라우트라, 라우트 추가 이전에 뜬 서버에 붙으면
서버가 SPA fallback으로 index.html(HTML)을 돌려준다. 그대로 r.json()을 부르면
"Unexpected token '<'"만 보여서 사용자는 패널이 깨진 줄 안다. content-type을 먼저 보고
"서버가 구버전"이라고 정확히 알린다 — 앱 재시작이 해법임을 화면에서 바로 알 수 있게.
/

출처: `.ai_monitor/vibe-view/src/components/panels/DaemonsPanel.tsx:39`

## `.ai_monitor/vibe-view/src/components/panels/MemoryPanel.tsx`

### 모듈 상단 `[과거사고]`

/**
검색, CRUD, 폴링 로직을 포함한 독립 컴포넌트
- 2026-08-15 Claude: "총 N개"가 DB 전체 개수로 오해되던 표기 수정 + 작성자 필터 가시화.
[과거사고] 작성자 필터가 'user'로 걸린 채 4건만 뜨자 "공유 메모리가 죽었다"로 읽혔다.
실제로는 hive 666·zettel 3956건이 있고 회상도 7일 266회 hit. 원인은 두 가지였다 —
① 한 페이지(top 20)를 총계처럼 표기 ② 활성 필터가 9px 회색이라 안 보임.
- 2026-03-01 Claude: DB 정보 표시 + APPDATA→로컬 동기화 버튼 추가
배포 버전에서 어떤 DB를 사용 중인지 표시하고 수동 동기화 지원
- 2026-03-01 Claude: App.tsx에서 분리 — 독립 컴포넌트화
/

출처: `.ai_monitor/vibe-view/src/components/panels/MemoryPanel.tsx:1`

## 확인법

```bash
python scripts/wiki_lint.py        # 이 페이지의 출처가 아직 살아 있는지
python scripts/wiki_build.py       # 원본 주석 변경분 재합성
```

<!-- tags: 과거사고 -->
