---
title: 서버와 라우팅 함정
type: 함정
sources:
  - .ai_monitor/api/hive_api.py:362
  - .ai_monitor/api/hive_api.py:764
  - .ai_monitor/api/hive_api.py:814
  - .ai_monitor/server.py:723
  - .ai_monitor/server.py:1252
  - .ai_monitor/server.py:1754
  - .ai_monitor/server.py:2037
  - tests/test_route_table.py:120
  - incident_ledger  # 사고 12건
related: []
confidence: high
updated: 2026-08-15
---

# 서버와 라우팅 함정

## 한 줄

2026-06-21까지 이 핸들러는 레거시 task_logs.jsonl을 읽었음.

> 자동 합성 (코드 주석 8건 · 파일 3개 · 사고 장부 12건 · 추출 7ca29da).
> 🔴 **여기를 고치기 전에** 원본(주석 또는 사고 장부)을 먼저 고칠 것 — 다음 빌드에 덮어써진다.

## 🔴 밟았던 것 (사고 장부)

### 터미널 헤더 쿼터 배지 미표시 — /api/agent-quota가 index.html 응답

- **원인** — hive_api.py에 핸들러만 추가하고 server.py GET 디스패치 allowlist 미갱신 → SPA 폴백
- **수정** — server.py allowlist에 /api/agent-quota 추가. 규칙: hive_api 단건 라우트 추가 시 server.py 튜플 동기 갱신 필수 (`3e54260`)
- 출처: `incident_ledger` · 최초 2026-07-04

### block 치환 스크립트가 server.py 1115줄 오삭제 (GET 치환 후 첫 매칭이 위임줄로 바뀌어 POST 탐색 범위가 do_GET~do_POST 통째)

- **원인** — python 라인범위 스크립트로 동일 문자열('/api/projects') 2개 블록 순차 치환 — 첫 치환이 앵커를 오염시켜 두번째 범위 폭주
- **수정** — git checkout 복구 후 Edit 도구로 고유 컨텍스트 포함 정확 매칭 치환. 교훈: 동일 앵커 다중 블록은 스크립트 말고 Edit 고유매칭
- 출처: `incident_ledger` · 최초 2026-07-05

### AUTO heartbeat 토글 칩 클릭 무반응

- **원인** — 신규 /api/heartbeat GET가 server.py의 기존 liveness 엔드포인트(_g_heartbeat, exact-first)에 가려지고, /api/heartbeat/toggle POST는 _cp_hive 접두사(hive/orchestrator/superpowers)에 안 걸려 둘 다 hive_api에 미도달
- **수정** — GET는 /api/heartbeat/status로 개명 후 _cg_hive allowlist에 추가, POST는 _cp_hive에 exact 추가, App.tsx fetch 경로 동기+dist 재빌드
- 출처: `incident_ledger` · 최초 2026-07-17

### 프로젝트 폴더 선택해도 활성 프로젝트 안 잡힘(패널 빔+배너 안 사라짐)

- **원인** — PROJECT_CONTEXT_UNRESOLVED/DB커넥션/PROJECT_ROOT가 부팅 시 1회 고정, 런타임 재초기화 경로 없음. select-folder는 last_path만 저장
- **수정** — server.py _switch_project + POST /api/switch-project 신설(DB전환+스키마+배너해제+fs재지정+롤백). zettel_sync resolve 콜러블로 라이브 추종. App.tsx openFolder가 switch-project 호출 (a54ba4a)
- 출처: `incident_ledger` · 최초 2026-07-19

### 메시지 전송마다 최대화 창이 이전 크기로 축소

- **원인** — hook_bridge가 .server.pid만 조회해 살아있는 서버(.dev_server.pid)를 오프라인 오판 → 매 메시지 server.py 재스폰 → 새 인스턴스가 instance_lock에서 os._exit 직전 ShowWindow(hwnd, SW_RESTORE)를 무조건 호출(SW_RESTORE는 최대화 해제 효과)
- **수정** — instance_lock: IsIconic() 검사 후 최소화 창만 복원. hook_bridge: _start_server 진입부에 _is_server_alive 선체크로 재스폰 차단 (7fbbff2)
- 출처: `incident_ledger` · 최초 2026-07-25

### 콘솔창(터미널 슬롯)이 전부 튕김 — node PTY 서버 사망 후 워치독 강제 재시작

- **원인** — 앱이 타 프로젝트(D:/ons)를 활성 프로젝트로 열어둔 상태에서 hook_bridge가 /api/project-info 슬러그 대조 실패로 '내 서버 없음' 판정 -> server.py 스폰. 단일 인스턴스 락은 project_root 시드라 슬러그가 다르면 안 걸려 2번째 인스턴스가 끝까지 부팅, 자체 PTY 서버가 9000번대 포트 슬롯을 경쟁
- **수정** — hook_bridge._start_server에 _is_app_server_running() 가드를 _is_already_running보다 앞에 배치 — 앱 생존 중에는 스폰 금지하고 fallback 경로로. 회귀 테스트 5건(tests/test_hook_server_spawn_guard.py). 커밋 e62a7e2
- 출처: `incident_ledger` · 최초 2026-08-02

### Discord 릴레이 응답이 안 오거나 '제한 시간 안에 응답이 오지 않았습니다'로 끝남 (response_timeout)

- **원인** — 응답 수거가 2단(서버가 버스에 남김 → 게이트웨이가 폴링 수거)인데 대기 상한이 각자 하드코딩돼 어긋남: 게이트웨이 180초 vs 릴레이 600초. 게이트웨이가 먼저 포기하면 그 뒤 서버가 남긴 답변을 수거할 주체가 없어 DB에만 남고 영구 유실. 겹친 증상으로 헤드리스 claude는 턴 종료까지 무음이라 사용자가 '끊겼다'고 오인
- **수정** — src/connector_core.py에 RELAY_TIMEOUT_SEC/GATEWAY_WAIT_SEC 단일 원천 신설(불변식: 게이트웨이 > 릴레이), 양쪽에서 import. 게이트웨이에 20초 후 경과 표시 1건 생성 + 30초마다 편집 갱신 + 완료 시 결과로 덮어쓰기. 커밋 bb73c84
- 출처: `incident_ledger` · 최초 2026-08-06

### 학습 모니터(train_monitor.py:8899)가 반복해서 죽음 — 포트 LISTENING인데 응답 없음

- **원인** — _cmdlines() 캐시 TTL 2.0초 < 실제 소요 2.10초 → 캐시 영구 미스로 요청당 process_iter 4회(8.4초) + v41() 무캐시 5.18초 = /api 11.8초. 화면은 2초 폴링이라 요청이 쌓여 스레드 폭증
- **수정** — TTL 2.0→20.0초(측정 소요시간보다 크게), v41() 60초 캐시 분리(_v41_uncached), 폴링 2000→5000ms. 결과 11.8초→웜 0.15초/콜드 4.9초
- 출처: `incident_ledger` · 최초 2026-08-06

### 바이브코딩 앱이 시스템 메모리·CPU를 지속 잠식 (server.py Private 932MB, CPU 평균 13%)

- **원인** — zettel_sync.export_to_vault가 내용 변화와 무관하게 write_text를 무조건 호출 → 60초마다 vault 943개 mtime 갱신 → mtime 비교로 동작하는 mirror_vault가 GDrive로 전량 재복사 → import가 되읽는 3단 핑퐁. 두 스레드가 server.py CPU의 47% 점유
- **수정** — _write_if_changed 헬퍼로 무변경 파일 쓰기 스킵. updated 프론트매터는 datetime.now라 매번 달라지므로 volatile_prefixes로 비교에서만 제외. mirror_vault는 미수정(mtime 보존되면 자동 정지). 검증: 3회 export에 2·3회차 기록 0개, mtime 873개 전부 보존
- 출처: `incident_ledger` · 최초 2026-08-07

### 경량 소스 업데이트가 화면(UI) 변경을 전혀 전달하지 못함 — .py만 새것이고 UI는 옛것

- **원인** — 2겹. (1) server.py STATIC_DIR이 frozen에서 BASE_DIR=sys._MEIPASS(EXE에 구워진 사본)만 봄 — 경량 채널은 체크아웃 .py만 갈아끼우므로 UI는 영원히 옛 번들. (2) .ai_monitor/vibe-view/.gitignore의 dist 한 줄이 루트 .gitignore의 !.../dist/** 예외를 무력화 — git은 더 깊은 .gitignore가 이긴다. 신규 빌드 산출물은 커밋 자체가 불가였고 추적 중이던 dist 자산 20개는 전부 화석
- **수정** — 중첩 .gitignore에서 dist 제거 + STATIC_DIR이 체크아웃 dist 우선(깨졌으면 번들 폴백). _dist_is_usable은 index.html 존재만이 아니라 참조 번들 실재까지 확인 — 반쪽 dist를 서빙하면 화면이 빈 채로 뜬다
- 출처: `incident_ledger` · 최초 2026-08-07

### 앱 상시 느림 — 시스템 CPU 40% 고착

- **원인** — YAML 이스케이프 왕복 버그의 잔존 데이터. 제목이 왕복마다 2배씩 자라 최대 115MB(vibe-2781), 볼트가 로컬1631MB+GDrive1627MB로 부품. ZettelGDrive 데몬이 120초마다 전량 read_text+정규식 파싱 -> server.py CPU 93% 점유, 메모리 2352MB, Defender가 그 읽기를 스캔해 17% 추가
- **수정** — fix_corrupted_titles.py 복구범위를 GDrive볼트까지 확장(로컬만 고쳐 되살아나던 구멍) + 1MB초과는 삭제해 export가 재생성 + zettel_sync에 MAX_TITLE_CHARS/MAX_NOTE_FILE_BYTES 방어선(read_text 전 stat으로 차단). 커밋 c93061f
- 출처: `incident_ledger` · 최초 2026-08-08

### 음성 설정이 '음성 엔진 준비 중…'에서 안 넘어감 (TTS 로딩 실패: [Errno 22] Invalid argument)

- **원인** — voice_api._spawn_sidecar가 stdout=PIPE로 사이드카를 띄우는데 부모(server.py)가 읽어주는 스레드가 없어 파이프 버퍼가 차 자식이 멈춤. 지문=CPU 0.03초·리슨 포트 0개·프로세스는 살아있음. 게다가 _spawned=True 플래그가 죽은 자식에도 남아 재기동을 영구 차단. 같은 결함을 postgres_runtime.py:212, agent_api.py:18이 이미 고쳤는데 음성만 빠짐
- **수정** — stdout을 voice-server/voice-server.log 파일로 리다이렉트(DEVNULL 아님 — 콘솔 없는 자식이라 버리면 진단 불가), stdin=DEVNULL(부모 pythonw는 stdin 핸들 무효), _spawned bool → _proc Popen + poll()로 실물 판정. 커밋 bb07263
- 출처: `incident_ledger` · 최초 2026-08-15

## 코드에 박힌 지식

## `.ai_monitor/api/hive_api.py`

### handle_get `[과거사고]`

── /api/hive/activity ──────────────────────────────────────────────
pg_logs에서 하이브 시스템 사용 이벤트만 분류하여 반환.
대시보드 TerminalSlot "하이브 상태" 위젯이 3초 폴링으로 사용.
[과거사고] 2026-06-21까지 이 핸들러는 레거시 task_logs.jsonl을 읽었음.
해당 파일은 PostgreSQL-first 전환(CLAUDE.md 규칙4) 시점 2026-03-01에
기록이 끊겨 동결 → 위젯이 "오후 03:54"(=3월1일 마지막 이벤트, fmtTime이
날짜를 숨겨 착시)에 멈추고 memory_write 0건으로 "없음" 표시되는 버그.
진실의 원천인 pg_logs(실시간)로 교체.

출처: `.ai_monitor/api/hive_api.py:362`

### handle_get `[과거사고]`

── /api/heartbeat/status ───────────────────────────────────────────
자율 heartbeat 상태 — 대시보드 헤더 토글 칩용
[과거사고 2026-07-17] 최초 구현이 이 경로를 '/api/heartbeat'로 잡았다가 server.py의
liveness 엔드포인트(_g_heartbeat, exact-first)에 완전히 가려져 칩이 영구 OFF.
'/api/heartbeat'는 useVibeData·hive_watchdog가 쓰는 생존신호 → 절대 재점유 금지.

출처: `.ai_monitor/api/hive_api.py:764`

### check_exists `[과거사고]`

[과거사고 2026-08-01] hive_tasks.updated_at은 timestamp가 아니라 **text**(ISO 문자열).
to_char(text, ...) 오버로드가 없어 매 폴링마다 쿼리가 통째로 실패했고,
query_rows가 예외를 삼켜 화면엔 "0건"으로만 보였다(무증상). 대신 server.log에
에러+HINT 4줄이 폴링 주기마다 쌓여 48시간에 4,800건/로그 61MB로 불어났다.
→ ::timestamptz 캐스트로 오버로드를 맞춘다. 컬럼 타입을 바꾸면 이 값을 문자열로
비교/저장하는 다른 경로가 깨지므로 쿼리 쪽에서 캐스트하는 편이 안전하다.

출처: `.ai_monitor/api/hive_api.py:814`

## `.ai_monitor/server.py`

### 모듈 상단 `[과거사고]`

[2026-06-21] frozen(설치본) 프로젝트 컨텍스트 자동 고정 — 회귀 사고 방지.
[과거사고] 설치 버전에서 하이브 마인드/제텔카스텐/태스크 패널이 통째로 비어 보이던 버그.
근본 원인: 설치본 config.json(%APPDATA%\VibeCoding)의 last_path가 활성 프로젝트를 안 가리키면
project_id가 install-dir 슬러그(phantom)로 잡혀, project_id로 필터되는 모든 데이터가 0건 조회됨.
(DB는 dev/설치 공유라 데이터는 D--vibe-coding로 멀쩡히 저장돼 있는데 네임스페이스만 어긋남)
[대책] 시작 시 PROJECT_ROOT가 실제 프로젝트(.git/CLAUDE.md/GEMINI.md 마커 보유)로 해석됐으면
last_path를 거기에 자동 고정 + projects.json 동기화 → 프론트(FileExplorer가 /api/config last_path로
currentPath 설정)와 백엔드 기본 project_id가 항상 정렬됨. 마커 없는 install-dir 폴백이면
PROJECT_CONTEXT_UNRESOLVED 플래그를 세워 UI가 "프로젝트 선택"을 유도하도록 노출(빈 패널 미스터리 방지).

출처: `.ai_monitor/server.py:723`

### 모듈 상단 `[과거사고]`

hive: prefix2 + exact8 혼합 → hive_api 위임.
[과거사고 2026-07-04] agent-quota 누락 — hive_api에 핸들러 추가하고 이 allowlist를 안 갱신해
SPA 폴백(index.html)이 응답 → 쿼터 배지 미표시. hive_api 단건 라우트 추가 시 이 튜플도 동기 갱신.

출처: `.ai_monitor/server.py:1252`

### do_POST `[과거사고]` `[불변식]`

미매칭 POST → 404. 모든 라우트가 exact/prefix/cond 테이블에서 처리됨 — 여기 도달 = 미등록 경로.
[과거사고 2026-08-05] 예전에는 여기서 본문을 읽지 않고 헤더만 보낸 뒤 닫았다.
BaseHTTPRequestHandler는 HTTP/1.0(Connection: close)이라 응답 직후 소켓을 닫는데,
수신 버퍼에 안 읽은 요청 본문이 남아 있으면 Windows가 RST를 보낸다. curl은 이미
응답을 파싱해서 404로 보이지만, 브라우저 fetch는 응답을 버리고 TypeError로 실패한다
→ UI에 원인 불명의 'Failed to fetch'만 뜬다. 실제로 설치본에서 구버전 백엔드가
신규 라우트(당시 /api/config/discord — 지금은 제거됨)를 모르는 상황이 정확히
이렇게 보여서, 라우트 부재라는 진짜 원인이 네트워크 장애로 오인됐다.
[불변식] 응답 전에 본문을 반드시 비운다. 본문 있는 POST에만 해당되므로 GET 폴백엔 불필요.

출처: `.ai_monitor/server.py:1754`

### main `[WHY]` `[과거사고]`

--- 경량 소스 업데이트 채널 폴링 (boot.py A안, non-blocking) ---
[WHY] EXE 풀빌드 채널과 별개로 main 커밋 SHA를 주기 폴링 — 순수 .py push를 빠르게 감지.
[과거사고 2026-07-03] "dev는 체크아웃 아니라 무해" 가정이 틀렸음 — dev 트리도 체크아웃이라
ready=true 배너→apply가 dev 트리를 reset --hard(미푸시 커밋 4개 고아화). 지금은
soft_updater._channel_block_reason이 비frozen 실행을 채널에서 차단(VIBE_SRC_DIR opt-in 제외).
[자동 적용 2026-08-07] 감지에서 멈추지 않고 **받아두기(stage)** 까지 여기서 끝낸다.
느린 fetch를 앱이 떠 있는 동안 처리해야 다음 부팅에서 boot.py가 로컬 reset만으로
즉시 최신이 된다(= 재시작 1번, 부팅 지연 0). 트리 이동은 안 하므로 실행 중 코드는 불변.

출처: `.ai_monitor/server.py:2037`

## `tests/test_route_table.py`

### test_post_404_fallback_drains_request_body `[과거사고]`

[과거사고 2026-08-05] 본문을 안 읽고 닫으면 Windows가 RST를 보내 브라우저 fetch가
'Failed to fetch'로 실패한다. 404 본문조차 못 읽어 '라우트 없음'이라는 진짜 원인이
네트워크 장애로 오인된다(설치본 Discord 설정 저장 실패의 실제 증상).

출처: `tests/test_route_table.py:120`

## 확인법

```bash
python scripts/wiki_lint.py        # 이 페이지의 출처가 아직 살아 있는지
python scripts/wiki_build.py       # 원본 주석 변경분 재합성
```

<!-- tags: WHY, 과거사고, 불변식 -->
