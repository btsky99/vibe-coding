---
title: 업데이트와 배포 함정
type: 함정
sources:
  - .ai_monitor/boot.py:160
  - .ai_monitor/boot.py:363
  - .ai_monitor/soft_updater.py:69
  - .ai_monitor/soft_updater.py:89
  - .ai_monitor/updater.py:57
  - .ai_monitor/updater.py:103
  - .ai_monitor/updater.py:215
  - scripts/smoke_test.py:120
  - scripts/smoke_test.py:147
  - scripts/smoke_test.py:160
  - scripts/smoke_test.py:275
  - incident_ledger  # 사고 17건
related: []
confidence: high
updated: 2026-08-15
---

# 업데이트와 배포 함정

## 한 줄

의존성/C확장/부트스트랩이 바뀐 소스를 옛 EXE가 받아 실행하면

> 자동 합성 (코드 주석 11건 · 파일 4개 · 사고 장부 17건 · 추출 55d5cf1).
> 🔴 **여기를 고치기 전에** 원본(주석 또는 사고 장부)을 먼저 고칠 것 — 다음 빌드에 덮어써진다.

## 🔴 밟았던 것 (사고 장부)

### 설치 버전에서 새 릴리즈가 나와도 업데이트 알림이 안 뜸 (무음 실패)

- **원인** — private 리포 시절 토큰이 설치 PC github_token.txt/GITHUB_TOKEN에 잔존 — 만료 토큰으로 GitHub API 401 → _fetch_latest_release가 None 반환 후 무음 종료
- **수정** — updater._fetch_latest_release: 401/403 시 토큰 없이 1회 재시도 (공개 리포라 무토큰 동작) + 실패 사유를 update_ready.json last_error로 기록 + check-update-ready가 진단 파일 보존
- 출처: `incident_ledger` · 최초 2026-06-11

### Inno Setup 설치 중 레지스트리 에러 팝업 — RegCreateKeyEx 액세스 거부(코드 5), VIBE_HIVE_HOOK HKLM 쓰기 실패

- **원인** — .iss [Registry] HKLM 쓰기에 IsAdminInstallMode 가드 부재 — 비관리자(현재 사용자용) 설치에서 권한 거부 (v3.7.226 a0cd58a 도입)
- **수정** — HKLM 항목에 Check: IsAdminInstallMode 추가 + 비관리자용 HKCU\Environment 폴백 항목 추가 (`2ff9eb5`)
- 출처: `incident_ledger` · 최초 2026-06-11

### 설치(frozen) 앱에서 자동 업데이트가 전혀 안 됨 — 업데이트 알림 미표시 + 수동 체크 무반응

- **원인** — updater.py가 vibe-coding.spec datas와 build-release.yml --add-data 양쪽에서 누락 → frozen 번들 MEIPASS에 미포함 → 'from updater import check_and_update'가 ImportError → 시작 루프(server.py:4054)와 수동 트리거(2019) 모두 조용히 skip. 실행 중 설치 EXE의 _MEI 추출 폴더에서 updater.py 부재 확정(_version/api/infra는 존재)
- **수정** — spec datas에 ('.ai_monitor/updater.py','.') + hiddenimports 'updater' 추가, build-release.yml GUI/console 빌드 양쪽에 --add-data 'updater.py;.' + --hidden-import updater 추가 (_version.py 패턴 미러링)
- 출처: `incident_ledger` · 최초 2026-06-21

### soft-update apply가 dev 트리(D:\vibe-coding)를 reset --hard origin/main — 미푸시 커밋 4개(de2e6c6~edd882e) 고아화

- **원인** — dev 트리도 git 체크아웃이라 _is_checkout 통과 → 미푸시 커밋 때문에 local!=remote → ready=true 배너 → apply 실행. 'dev는 체크아웃 아님' 가정 오류
- **수정** — reflog+.soft_rollback로 ff 복구 + soft_updater에 가드 2겹: 비frozen 채널 차단(_channel_block_reason) + merge-base --is-ancestor 유실 방지
- 출처: `incident_ledger` · 최초 2026-07-03

### soft 채널 apply가 dev 트리(D:\vibe-coding)를 reset --hard — 미푸시 커밋 4개 고아화

- **원인** — dev 트리도 git 체크아웃이라 _is_checkout 통과 + 미푸시 커밋으로 local≠remote → ready=true 배너 → apply 실행. 채널 자격(frozen 관리 체크아웃 전용) 검사 부재
- **수정** — 가드 2겹: (1) _channel_block_reason — 비frozen 실행은 채널 차단(VIBE_SRC_DIR opt-in 예외) (2) apply 직전 merge-base --is-ancestor로 로컬 전용 커밋 감지 시 reset 차단. 복구는 .soft_rollback SHA로 완료
- 출처: `incident_ledger` · 최초 2026-07-03

### soft update apply가 dev 트리(D:\vibe-coding)를 reset --hard origin/main — 미푸시 커밋 4개(edd882e~21a8f02) 고아화, UI/dist도

- **원인** — 가드(eb9a53e)는 디스크에 있었지만 실행 중이던 서버 프로세스가 가드 추가 전에 기동돼 메모리에 옛 soft_updater를 캐시 — 배너 apply 클릭 시 옛 코드가 실행됨
- **수정** — .soft_rollback의 SHA(21a8f02)로 ff-merge 복구 + 자동생성 문서 충돌 HEAD 해소 + vibe-view 재빌드. 교훈: soft 채널 가드 수정 후엔 반드시 앱 재시작 필요
- 출처: `incident_ledger` · 최초 2026-07-04

### 업데이트 버튼 후 Failed to load Python DLL + Failed to remove temporary directory 재발 (v3.7.247, 다중 PC)

- **원인** — 인수인계 중 이전 인스턴스 node PTY 서버가 채 안 죽고 runtime\_MEI\pty-server를 잠금 → 새 EXE onefile 부트로더 추출이 잠긴 잔여물과 충돌. 좀비 정리가 종료(atexit)+updater os._exit 직전에만 있고 부팅/인수인계 시점 청소 부재. setup 새설치는 runtime 깨끗해서 무증상
- **수정** — _update.bat이 구 프로세스 사망 후 새 EXE 기동 전에 runtime 고아 node kill + 깨진 _MEI(python DLL 누락) 삭제(setup 새설치와 동일 상태). +heal_broken_mei_at_startup 부팅 자가치유. +다운로드 percent 진행바. v3.7.248
- 출처: `incident_ledger` · 최초 2026-07-09

### 설치버전 아이콘 눌러도 무반응 (작업관리자 깜빡임 없음)

- **원인** — onefile→onedir per-user 전환으로 exe 경로가 %LOCALAPPDATA%\VibeCoding\app → %LOCALAPPDATA%\Programs\VibeCoding 로 이사. silent 업데이트가 desktopicon(기본 unchecked) task를 재실행 안 해 옛 바로가기가 죽은 옛 경로를 계속 가리킴
- **수정** — vibe-coding-setup.iss [Run]에 죽은 VibeCoding 바로가기 자동 repoint + InitializeSetup에 구 빈 폴더 청소 추가 (커밋 fffd51c)
- 출처: `incident_ledger` · 최초 2026-07-10

### 업데이트 클릭 시 설치 마법사가 뜨고 DeleteFile 실패 코드32(사용 중)

- **원인** — updater가 setup 인스톨러를 vibe-coding.exe.new로 저장 → is_installer_asset이 'setup' 미검출로 onefile 오판 → apply_update_from_temp가 인스톨러를 {app}\vibe-coding.exe로 스왑 → 실행 시 자기 자신 덮어쓰기
- **수정** — _find_asset(dict 반환)로 원본 에셋명 판정 + tmp를 vibe-coding-setup.exe.new로 이름지어 인스톨러 여부 보존 + update_ready.json에 is_installer 명시 저장 + apply가 그 플래그 신뢰
- 출처: `incident_ledger` · 최초 2026-07-12

### git push rejected + 릴리즈 태그 v3.7.261 이미 존재 (auto_version 결과가 원격 CI 자동범프 bcf7565와 충돌)

- **원인** — 로컬에 미푸시 커밋 11개가 쌓인 채 auto_version.py 실행 — CI가 릴리즈마다 자동범프 커밋([skip ci])+태그를 원격에 남기므로 로컬 버전 계산이 원격과 어긋남
- **수정** — git pull --rebase --autostash로 원격 자동범프 흡수 → auto_version.py 재실행(262)으로 태그 충돌 회피 후 푸시. 예방: 릴리즈 전 git fetch로 원격 태그/자동범프 선확인
- 출처: `incident_ledger` · 최초 2026-07-16

### smoke test EXE가 taskkill /F에도 안 죽는 커널 좀비化 (HasExited=True, 스레드 1, 핸들 714) → 고정 smoke 락 포트 19067 영구 점유 → 이후 모든 

- **원인** — 이중 결함: ① smoke_test.py finally의 proc.wait(10)이 try 밖 — 못 죽인 EXE에서 TimeoutExpired가 전파되며 고아 방치. ② instance_lock smoke 시드가 project_root 고정 — 좀비 1개가 후속 smoke 전멸시키는 단일 장애점
- **수정** — smoke_test.py wait를 try+kill 에스컬레이션으로 보호, instance_lock smoke 시드에 PID 추가로 실행별 고유화(좀비 내성). 점유 중인 좀비 포트는 재부팅 전까지 미해제 — Bound 소켓은 netstat에 안 보이니 Get-NetTCPConnection(상태 무필터)로 확인
- 출처: `incident_ledger` · 최초 2026-07-16

### psql --tuples-only --csv에서 UPDATE ... RETURNING 0행 시 stdout에 'UPDATE 0' 커맨드 태그 잔류 → base64 디코드 실패

- **원인** — psql은 tuples-only여도 DML 커맨드 태그를 stdout에 출력 — RETURNING 데이터와 섞임
- **수정** — telegram_bridge._flush_heartbeat_outbox에서 'UPDATE ' 접두 라인 필터 후 base64 파싱
- 출처: `incident_ledger` · 최초 2026-07-17

### 새 릴리즈(v3.7.313)를 푸시해도 설치본에 업데이트 배너가 안 뜸

- **원인** — updater.py의 from _version import __version__가 boot.py의 sys.path 최우선 체크아웃을 잡아 번들(3.7.312) 대신 소스(3.7.313) 버전을 읽음 → _is_newer 항상 False → update_ready.json 삭제
- **수정** — bundle_version() 추가(_MEIPASS 우선, __file__ 후보는 마지막)해 check_and_update가 번들 버전으로 비교 (fd35229)
- 출처: `incident_ledger` · 최초 2026-08-01

### 경량 소스 업데이트 apply가 유실방지 가드에 막힘 (관리 체크아웃에 로컬 전용 커밋 1개)

- **원인** — %LOCALAPPDATA%\VibeCoding\app 체크아웃에 upstream c7a42f2와 내용이 완전히 동일한 중복 커밋 e28b108이 생성돼 있었음(2026-08-07 18:47). ancestor 가드가 '로컬 전용 커밋 있음'으로 보고 apply 차단. 누가 이 체크아웃에 커밋했는지는 미규명 — 재발하면 soft 업데이트가 매번 조용히 막힌다
- **수정** — patch-id 및 git diff origin/main HEAD -- <파일> 로 내용 동일함을 먼저 확인 → 백업 브랜치(backup/pre-align-2026-08-07) 생성 후 reset --hard origin/main. 확인 없이 reset 금지(2026-07-03 유실 사고)
- 출처: `incident_ledger` · 최초 2026-08-07

### 진행률 바가 뒤로 감 (8% -> 4%)

- **원인** — git --progress는 단계마다 0->100%를 반복한다. Enumerating objects와 Counting objects에 같은 구간(0-8)을 배분했더니 Enumerating 100% 다음 Counting 50%에서 역행
- **수정** — 구간을 겹치지 않게 분리(Enumerating 0-4 / Counting 4-8) + 최댓값 단조 클램프. 진행바 역행은 사용자가 실패로 읽으므로 정확도보다 단조성이 우선
- 출처: `incident_ledger` · 최초 2026-08-07

### 설치본에서 LAN 브리지 '켜기'가 동작하지 않음 (토글 후 재시작해도 브리지 미기동, 오류도 없음)

- **원인** — run_lan_bridge가 실행기를 runtime.python_runner_cmds()[0]로 골랐는데 이 함수는 frozen에서 앱 EXE를 후보에서 일부러 제외한다. Python 미설치 PC는 마지막 폴백 'python'이 나가 proc.popen이 FileNotFoundError로 사망. 데몬 스레드에 try가 없고 frozen GUI는 stderr가 어디에도 안 붙어 흔적이 0. 개발 PC에는 Python이 깔려 있어 우연히 떠서 '개발에선 되는데 설치본에선 안 됨'으로 갈렸다.
- **수정** — runtime.script_runner_cmd() 신설 — frozen이면 sys.executable(앱 EXE, boot.py가 .py 인자를 runpy로 받음). run_lan_bridge/run_watchdog/run_codex_pg_watcher가 이걸 사용. _spawn_script로 OSError 포착 후 실행기 경로를 로그. 방화벽(netsh) 등록용 관리자 권한은 바로가기 .lnk 바이트 패치(0x15|=0x20)로 자동 체크.
- 출처: `incident_ledger` · 최초 2026-08-14

### 앱 실행 시 하얀 화면 + 재실행 불가 (WebView2 0x80010108 RPC_E_DISCONNECTED)

- **원인** — WebView2 런타임이 151.0.4129.78→.86 자동 업데이트됐는데, 창을 잃은 옛 앱 프로세스가 UDF를 구버전으로 점유 중이어서 CreateCoreWebView2ControllerAsync 실패. pywebview는 이 실패를 예외로 안 올려 창만 하얗게 남고, 그 프로세스가 단일 인스턴스 락을 계속 쥐어 재실행조차 막힘
- **수정** — infra/webview_health.py 신설 — CoreWebView2 생성 감시, 실패 시 런타임버전 vs UDF Last Version 비교 진단을 MessageBox로 표시하고 락 해제 후 os._exit. 즉시 복구는 UDF 점유 msedgewebview2 종료
- 출처: `incident_ledger` · 최초 2026-08-15

## 코드에 박힌 지식

## `.ai_monitor/boot.py`

### _gate_ok `[과거사고]`

SRC의 soft_manifest.min_exe 가 현재 EXE 버전보다 높으면 차단(False).
[과거사고 방지] 의존성/C확장/부트스트랩이 바뀐 소스를 옛 EXE가 받아 실행하면
ImportError/크래시 → min_exe 게이트로 풀빌드를 강제한다.
manifest 없거나 파싱 실패 시엔 통과(게이트는 명시적 차단만) — 구버전 소스 호환.

출처: `.ai_monitor/boot.py:160`

### _run_daemon_script `[과거사고]`

`vibe-coding.exe <script.py> [args]` — 데몬/도구 재실행 경로.
[과거사고/제약] frozen에서 server.py가 일부 데몬을 `[sys.executable, <script>, ...]`로
띄움 → sys.executable=EXE라 인자로 스크립트가 들어온다. 여기서 runpy로 받지 않으면
EXE가 다시 부팅 시퀀스를 타며 무한 창 생성(v3.7.47 유형) 사고가 난다.
clone 금지(부모 메인앱이 이미 SRC 보장). 절대경로면 그대로, 상대면 SRC 기준.

출처: `.ai_monitor/boot.py:363`

## `.ai_monitor/soft_updater.py`

### _channel_block_reason `[과거사고]`

soft 채널 활성 자격 검사 — 차단 사유 문자열, 통과면 None.
[과거사고 2026-07-03] dev 트리(비frozen)도 git 체크아웃이라 _is_checkout을 통과 →
미푸시 커밋 때문에 local≠remote → ready=true 배너 → apply가 dev 트리를
reset --hard → 커밋 4개 고아화(.soft_rollback로 복구). 채널은 boot.py가 만든
frozen 관리 체크아웃(%LOCALAPPDATA%\\VibeCoding\\app) 전용이다.
[예외] VIBE_SRC_DIR 오버라이드는 명시적 opt-in(E2E 테스트/특수 배치) — 허용.

출처: `.ai_monitor/soft_updater.py:69`

### _remote_head `[과거사고]`

GitHub API로 main 최신 커밋 SHA. 토큰 불필요(public)지만 rate-limit 완화용으로 사용.
[과거사고 updater.py:57] 만료 토큰(401/403) 시 토큰 없이 1회 재시도 — 무음 실패 방지.

출처: `.ai_monitor/soft_updater.py:89`

## `.ai_monitor/updater.py`

### bundle_version `[WHY]` `[과거사고]` `[불변식]`

실제 EXE 번들의 버전 — 풀빌드 업데이트 판단의 **유일한** 기준.
[WHY 모듈 상수 APP_VERSION과 분리] boot.py(경량 소스 채널 A안)가 관리 체크아웃의 .py를
runpy로 실행하며 sys.path 최우선을 체크아웃으로 잡는다 → 위쪽 `from _version import ...`은
번들이 아니라 **소스 버전**을 준다. soft 채널이 소스를 최신으로 당겨오면
소스 버전 == 최신 릴리즈 태그가 되어 _is_newer가 항상 False → EXE 풀빌드 업데이트가
영구히 안 뜬다(번들 의존성은 옛 버전에 멈춘 채로).
[과거사고 2026-08-01] 설치본 EXE 3.7.286 + 체크아웃 소스 3.7.313 → "이미 최신"으로
오판해 v3.7.313 배너 미표시. 소스만 갱신되는 구조에서는 두 버전이 상시 갈린다.
[불변식] frozen이면 _MEIPASS(onedir은 `_internal/`)의 _version.py가 진짜 번들 버전.
__file__ 기준 후보는 체크아웃 소스를 가리키므로 **반드시 마지막**에 온다.
_MEIPASS가 없으면(비frozen) 후보에서 제외 — Path("")/"_version.py"는 cwd 상대경로가 되어
엉뚱한 파일을 읽을 수 있다.

출처: `.ai_monitor/updater.py:57`

### _fetch_latest_release `[과거사고]`

GitHub Releases API에서 최신 릴리즈 정보를 가져옵니다.
[과거사고 2026-06-11] private 리포 시절 발급한 토큰이 설치 PC의
github_token.txt / GITHUB_TOKEN에 남아 만료(401/403)되면 업데이트 감지가
영구 무음 실패 → "설치 버전에서 업데이트가 안 뜸". 공개 리포는 토큰이
필요 없으므로 인증 실패 시 반드시 토큰 없이 1회 재시도한다.

출처: `.ai_monitor/updater.py:103`

### _download_asset `[과거사고]` `[불변식]`

릴리스 에셋을 dest 경로에 다운로드합니다.
progress_cb(downloaded_bytes, total_bytes)가 주어지면 청크마다 호출한다.
total_bytes는 Content-Length가 없으면 0(진행률 미상) — caller가 방어한다.
[과거사고 2026-08-05] 완결성 검사가 `total < 1MB` 하나뿐이라, 연결이 중간에 끊겨
401MB 중 58MB만 받아도 '완료'로 통과했다. 그 잘린 파일이 update_ready.json에
ready=True로 기록되고, 사용자가 업데이트를 눌러도 손상된 인스톨러라 아무 일도
일어나지 않았다(실측: %APPDATA%\\VibeCoding\\vibe-coding-setup.exe.new 58MB 잔존).
이 PC는 ECONNRESET이 반복되는 회선이라 대용량 에셋에서 중간 절단이 특히 잘 난다.
[불변식] 크기·해시 중 하나라도 어긋나면 실패로 처리한다 — 손상 파일을 ready로
올리는 것보다 업데이트가 안 뜨는 쪽이 훨씬 안전하다(설치 EXE 교체는 되돌리기 어렵다).

출처: `.ai_monitor/updater.py:215`

## `scripts/smoke_test.py`

### _sync_managed_checkout `[과거사고]`

[과거사고 2026-08-01] text=True는 Windows 기본 코덱(cp949)으로 디코딩한다 →
git이 UTF-8 한글 진행 메시지를 뱉으면 communicate의 리더 스레드가
UnicodeDecodeError로 죽고, 실패 시 읽어야 할 stderr가 통째로 사라진다
(returncode는 0이라 smoke는 그대로 PASS → 검증이 눈을 감음). 인코딩 고정 필수.

출처: `scripts/smoke_test.py:120`

### run_smoke_test `[과거사고]` `[불변식]`

── 데이터 디렉토리 격리 (2026-08-01) ──────────────────────────────────
[과거사고] frozen EXE는 %APPDATA%\VibeCoding을 데이터 디렉토리로 쓴다 — 즉 smoke가
**설치본과 같은 디렉토리**를 공유했다. 실측 피해:
· soft_update_ready.json 덮어씀 → 설치본이 그 시각에 업데이트를 확인한 것처럼 보여
"왜 업데이트가 안 뜨지" 오진을 유발
· orchestrator.pid 등 운영 PID 파일 덮어씀 → 설치본의 daemon 판정이 오염될 수 있음
포트는 VIBE_PORT_BASE로 격리했지만 데이터는 공유라 반쪽 격리였다.
[불변식] pgdata는 격리하지 않는다 — PG는 설치본과 공유하는 것이 정상이고(같은 포트로
접속), 여기서 분리하면 smoke가 빈 DB를 만들며 기동 시간이 크게 늘어난다.

출처: `scripts/smoke_test.py:147`

### run_smoke_test `[과거사고]` `[주의]`

[과거사고 2026-07-16 / R18~R20 한계 해소] boot.py는 managed checkout
(%LOCALAPPDATA%/VibeCoding/app — soft 업데이트가 마지막 적용한 구 소스)을 실행
→ 동기화 없이는 smoke가 "푸시하려는 내 소스"가 아닌 구 릴리즈 코드로 PASS를 내는
검증 무효 상태였다. 체크아웃을 로컬 HEAD로 fetch+reset(=soft 업데이트와 동일
메커니즘의 선적용)해 생산 경로 그대로 현재 소스를 검증한다.
[주의] VIBE_SRC_DIR로 dev 트리를 직접 물리는 방식은 불가 — frozen 전제(내장 PG
경로 등)와 충돌해 서버가 기동하지 않음 (2026-07-16 실측 60초 무바인딩).

출처: `scripts/smoke_test.py:160`

### run_smoke_test `[과거사고]`

[과거사고 2026-07-16] 이 wait가 try 밖이라 TimeoutExpired가 finally를 뚫고
전파 → smoke가 크래시하며 못 죽인 EXE를 조용히 고아로 방치 → 커널 좀비가
락 포트(19001+해시) 영구 점유. 못 죽였으면 크래시 대신 경고를 크게 남긴다.

출처: `scripts/smoke_test.py:275`

## 확인법

```bash
python scripts/wiki_lint.py        # 이 페이지의 출처가 아직 살아 있는지
python scripts/wiki_build.py       # 원본 주석 변경분 재합성
```

<!-- tags: WHY, 과거사고, 불변식, 주의 -->
