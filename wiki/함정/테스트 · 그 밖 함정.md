---
title: 테스트 · 그 밖 함정
type: 함정
sources:
  - tests/test_cli_install_recovery.py:129
  - tests/test_installed_script_runner.py:92
  - tests/test_lan_sandbox.py:181
  - tests/test_setup_doctor.py:56
  - tests/test_updater_release_path.py:232
  - incident_ledger  # 사고 4건
related: []
confidence: high
updated: 2026-08-15
---

# 테스트 · 그 밖 함정

## 한 줄

returncode만 믿고 '완료'를 찍어, 다음 진단에서 미설치로 돌아오는데도

> 자동 합성 (코드 주석 5건 · 파일 5개 · 사고 장부 4건 · 추출 909e7e6).
> 🔴 **여기를 고치기 전에** 원본(주석 또는 사고 장부)을 먼저 고칠 것 — 다음 빌드에 덮어써진다.

## 🔴 밟았던 것 (사고 장부)

### pytest tests/ 가 collection 에러로 테스트 0개 실행 — 안전망 무력화

- **원인** — test_ 함수 0개인 수동 playwright 스크립트가 test_*.py 이름으로 tests/에 존재, 시스템 파이썬에 playwright 부재로 import 실패가 수집 전체를 중단
- **수정** — office_*_check.py로 rename(git mv) — pytest 대상에서 제외, 수동 실행 용도 유지. 127 passed 복구 (커밋 cf94a06)
- 출처: `incident_ledger` · 최초 2026-07-14

### CI pytest 게이트 실패: handle_post() got an unexpected keyword argument 'SESSIONS_FILE' (test_new_api_modules.py 4건

- **원인** — 62f9b02(sessions.jsonl 레거시 미러 은퇴)에서 tasks_api.handle_post()의 SESSIONS_FILE 키워드 인자를 제거했으나 tests/test_new_api_modules.py는 옛 시그니처로 호출 지속. 레거시 제거 시 호출부(테스트 포함) 동반 갱신 누락.
- **수정** — 테스트 4곳(create/update/delete/claim)에서 SESSIONS_FILE 인자 삭제 + create_task 미사용 sessions.jsonl 셋업 제거. 로컬 pytest 127개 PASS 후 v3.7.264 재빌드 성공(8403078).
- 출처: `incident_ledger` · 최초 2026-07-19

### test_app_alive_skips_offline_fallback 실패 — capsys.out이 빈 문자열

- **원인** — hook_bridge.main()이 VIBE_CHILD_AGENT=1(캐스케이드 가드) 상속으로 출력 없이 exit 0. 테스트가 환경변수를 격리하지 않아 실행 셸에 따라 결과가 달라짐
- **수정** — tests/test_hook_server_spawn_guard.py에 autouse 픽스처로 OFFICE_MODE/VIBE_CHILD_AGENT delenv
- 출처: `incident_ledger` · 최초 2026-08-09

### get_central_config가 설정을 썼는데도 None을 반환

- **원인** — node_identity._read_config가 Path 전용인데 str을 받으면 AttributeError가 나고, 아래 except가 모든 예외를 {}로 삼켜 '설정 없음'으로 둔갑
- **수정** — Path(config_file)로 정규화 + tests/test_pg_central.py에 회귀 테스트(없는 파일은 여전히 None)
- 출처: `incident_ledger` · 최초 2026-08-11

## 코드에 박힌 지식

## `tests/test_cli_install_recovery.py`

### test_install_antigravity_fails_when_agy_missing `[과거사고]`

인스톨러가 exit 0이어도 `agy`가 없으면 실패로 보고해야 한다.
[과거사고] returncode만 믿고 '완료'를 찍어, 다음 진단에서 미설치로 돌아오는데도
설치 로그는 성공으로 남아 원인 추적이 막혔다.

출처: `tests/test_cli_install_recovery.py:129`

## `tests/test_installed_script_runner.py`

### test_spawn_failure_is_logged_not_silent `[과거사고]`

[과거사고] 실행기가 없으면 스레드가 통째로 사라져 흔적이 0이었다 —
사용자에겐 '토글을 켰는데 아무 일도 안 일어남'으로만 보였다."""

출처: `tests/test_installed_script_runner.py:92`

## `tests/test_lan_sandbox.py`

### test_settings_profile_is_absolute_and_denies_dangerous_commands `[과거사고]`

[과거사고] 상대경로 --settings는 claude가 cwd 기준 해석 후 미발견 시 exit 1."""

출처: `tests/test_lan_sandbox.py:181`

## `tests/test_setup_doctor.py`

### 모듈 상단 `[과거사고]`

═══════════════════════════════════════════════════════════════════════
check_hooks — settings.json 훅 자동 수리
[과거사고] 2026-07-30: check_hooks가 이벤트 배열의 원소를 flat {type,command}로 가정해
그룹 객체에 `.get("command")`를 읽어 항상 ''를 얻었다 → 이미 등록된 훅을 "누락"으로
오판하고 flat 항목을 append → CLI가 "hooks: Expected array, but received undefined"로
거부. 진단 API는 SetupBanner가 부팅마다 호출하므로 손으로 지워도 재오염됐다.
아래 테스트가 그 재발을 막는다.
═══════════════════════════════════════════════════════════════════════

출처: `tests/test_setup_doctor.py:56`

## `tests/test_updater_release_path.py`

### 모듈 상단 `[과거사고]`

═══════════════════════════════════════════════════════════════════════════
_download_asset — 절단/손상 다운로드 차단
[과거사고 2026-08-05] 완결성 검사가 `total < 1MB` 하나뿐이라 401MB 중 58MB만 받아도
'완료'로 통과했다. 손상 인스톨러가 ready로 올라가 업데이트 버튼이 무반응이 됐다.
═══════════════════════════════════════════════════════════════════════════

출처: `tests/test_updater_release_path.py:232`

## 확인법

```bash
python scripts/wiki_lint.py        # 이 페이지의 출처가 아직 살아 있는지
python scripts/wiki_build.py       # 원본 주석 변경분 재합성
```

<!-- tags: 과거사고 -->
